## 1. Baseline and prerequisites

- [x] 1.1 Record a redacted baseline for `/Users/androidteam/.hermes`: Hermes version/schema, profiles, gateway/launchd status, CLI and Telegram tool inventories, MCP status, approval settings, private URL/browser settings, memory/skill gates, file modes, state size, and relevant warning counts; do not read or print credential-file contents.
- [ ] 1.2 Complete the separately authorized rotation of any credential-like value found in non-secret settings, route the replacement through supported Hermes secret configuration, and verify only redacted presence and restrictive permissions.
- [x] 1.3 Record the approved operating policy: CLI/Desktop and authorized Telegram receive full technical capability and all enabled MCP operations; Telegram user authorization, secret redaction, immutable Hermes blocks, service scopes, repository rules, and action-specific authority remain in force.
- [ ] 1.4 With explicit local backup-generation approval, run `hermes backup --quick --label pre-full-access-activation`; verify its manifest contains expected critical files and has empty `failed_dbs` and `oversized_skipped`, and run SQLite integrity checks without reading secrets.
- [ ] 1.5 With explicit full-backup approval and a verified path outside Hermes home, run `hermes -p default backup --output <approved-path-outside-HERMES_HOME>`; require “Backup complete” with no skipped-file warnings, then verify `unzip -t` and Hermes marker files. Record that `hermes import <archive>` targets the default Hermes root, requires all Hermes processes stopped and destructive-restore approval, and MUST NOT be treated as a named-profile-only restore.
- [x] 1.6 Record separate apply checkpoints for credential rotation, package/network execution, configuration mutation, backup generation, restart of the existing launchd service, reversible local writes, provider/private-network calls, and shared-service mutations; do not treat baseline inspection as authorization for any later class.

## 2. Schema and common full-access configuration

- [ ] 2.1 Confirm the default profile's installed schema (reported as v37 on 2026-08-18, drifted from the v33 baseline) with `hermes config check`; schema drift has been reported by the installed runtime, so stop for explicit configuration-migration approval before running `hermes config migrate`, then inspect only the redacted result.
- [x] 2.2 Verify `delegation.max_async_children` is removed and `delegation.max_concurrent_children` is the single effective concurrency setting.
- [ ] 2.3 After explicit configuration-mutation approval, run `hermes config set timezone Asia/Ho_Chi_Minh` and `hermes config set agent.service_tier fast`; verify the invalid-zone and unsupported-tier warnings cease after activation.
- [ ] 2.4 Set `approvals.mode=off`, `approvals.cron_mode=approve`, `approvals.mcp_reload_confirm=false`, `approvals.destructive_slash_confirm=false`, and `delegation.subagent_auto_approve=true`. Run `hermes config unset approvals.deny`, `hermes config unset command_allowlist`, and `hermes config unset agent.disabled_toolsets`; do not use `config set ... '[]'`, which would store a string for these list-valued keys.
- [ ] 2.5 Configure autonomous state: `memory.write_approval=false`, `skills.write_approval=false`, memory and skill systems enabled, and background learning notifications/audit visibility retained.
- [ ] 2.6 Configure network/browser access with `security.allow_private_urls=true`, `browser.allow_private_urls=true`, `browser.allow_unsafe_evaluate=true`, `browser.restrict_evaluate=false`, and `security.website_blocklist.enabled=false`; retain `security.redact_secrets=true`.
- [x] 2.7 Keep `security.allow_lazy_installs=true`; verify Hermes' venv-scoped allowlisted lazy-install contract and stop before unrelated package installation without separate approval.
- [ ] 2.8 Preserve `compression.enabled=true` and set `tool_loop_guardrails.warnings_enabled=true`, `hard_stop_enabled=true`, `loop_caps.max_web_searches=50`, and `loop_caps.max_subagents=50`; verify the caps are nested under `loop_caps`.
- [ ] 2.9 Set delegation to `max_concurrent_children=3`, `orchestrator_enabled=true`, `max_spawn_depth=2`, `child_timeout_seconds=0`, and `max_iterations=50`; verify timeout zero disables only the wall-clock timeout while iteration/concurrency/loop controls remain active.
- [ ] 2.10 Resolve the custom endpoint's real context capacity and request/stale timeout behavior before setting any override. Current evidence is non-authoritative: `/v1/models` returned an authorization error, Hermes logged a 256,000-token probe fallback followed by a 1,050,000-token catalog heuristic, and the provider returned `context_too_large`. Leave `model.context_length` unset until the endpoint owner or an authenticated non-destructive probe establishes the limit.
- [ ] 2.11 Set `tools.tool_search.enabled=auto`, `threshold_pct=5`, `search_default_limit=5`, `max_search_limit=20`, `listing=auto`, and `listing_max_tokens=20000`; verify eligible MCP/plugin schemas defer without changing granted operations.
- [ ] 2.12 Set or verify `agent.gateway_timeout=1800`, `agent.gateway_timeout_warning=900`, `agent.gateway_notify_interval=180`, `agent.gateway_auto_continue_freshness=3600`, `agent.gateway_startup_restore_drain_timeout=30`, `agent.build_wait_timeout=600`, `agent.restart_drain_timeout=0`, and `gateway.delivery_ledger=true`.
- [ ] 2.13 Verify `curator.enabled=true`, `consolidate=false`, `stale_after_days=30`, `archive_after_days=90`, `backup.enabled=true`, and `backup.keep=5`. Run `hermes -p default curator run --dry-run` and `curator rollback --list`; with backup-generation approval, create and verify a labeled manual curator backup. Do not force consolidation or rollback during activation.

## 3. Enable every built-in capability

- [x] 3.1 Enumerate the installed Hermes toolset registry and runtime-gated tools for CLI and Telegram; save a non-secret inventory that distinguishes enabled, disabled, and unavailable-with-reason.
- [ ] 3.2 From the live inventory, run `hermes -p default tools enable --platform cli web browser terminal file code_execution vision video image_gen video_gen x_search tts skills todo memory context_engine session_search clarify delegation cronjob homeassistant spotify yuanbao computer_use` plus every installed CLI-applicable plugin key; do not pass unsupported `all`, `*`, STT, Project, or Kanban to this command.
- [ ] 3.3 Persist the Kanban exception by replacing one existing `platform_toolsets.cli` list element with `kanban` through `hermes -p default config set --force platform_toolsets.cli.<existing-index> kanban`, then re-run the complete `tools enable` command so the replaced configurable entry is restored and `kanban` is preserved. Verify both all 23 configurable names and `kanban` are stored and Kanban schemas register in an ordinary fresh CLI profile session.
- [ ] 3.4 Run the equivalent complete `hermes -p default tools enable --platform telegram ...` command, preserve `kanban` with the verified replace-then-re-enable sequence, and verify all configurable Telegram-applicable names plus Kanban without creating another profile.
- [ ] 3.5 Configure STT through `stt.enabled` and a separately approved provider; verify Project only in Desktop/GUI and `context_engine` only supplies tools when a non-default active engine is configured. Record Project as unsupported on CLI/Telegram without a Hermes source change.
- [x] 3.6 For each unavailable toolset, record the exact missing binary, credential, provider, plugin, platform, or setup hook; install/configure it only after contemporaneous approval for that package or external service action.
- [ ] 3.7 Start fresh CLI and Telegram sessions after tool schema changes and verify every available built-in tool is model-callable on its supported surface.
- [x] 3.8 Record the live prerequisite baseline: browser, terminal, file, code execution, vision, video, TTS, and Desktop Project tools are available in their supported surfaces; browser CDP, computer use, image/video generation, X search, Home Assistant, Spotify, Yuanbao, and other credential/plugin-gated integrations remain blocked until their reported prerequisites are supplied.

## 4. Preserve MCP Router and enable parallel calls

- [x] 4.1 Record the existing redacted MCP Router baseline: `npx -y @mcp_router/cli@latest connect`, all native tools enabled, no include/exclude filter, and no explicit resource/prompt/sampling/elicitation/parallel policy. Do not invoke or modify the server.
- [x] 4.2 Before profile/config mutation, compute a redacted structural fingerprint of `mcp_servers.mcp-router` that preserves key presence and non-secret command/argument structure without credential values.
- [ ] 4.3 With configuration-mutation approval, set only `mcp_servers.mcp-router.supports_parallel_tool_calls=true`; do not run `hermes mcp add/remove/configure/test`, `npx` package probes, package pinning, reload, or any other MCP setup mutation.
- [ ] 4.4 Set or verify `agent.parallel_tool_call_guidance=true` so independent built-in, plugin, and MCP calls are emitted together when appropriate.
- [ ] 4.5 Verify the post-change redacted MCP Router fingerprint differs only by the parallel-call flag and that the configured/registered tool inventory remains complete. Any other drift blocks activation.
- [ ] 4.6 In fresh CLI and Telegram sessions, verify representative independent read-only calls can be emitted together and each result remains associated with its call. Do not invoke `/reload-mcp` or shared-state mutations for this test.
- [x] 4.7 Record the accepted concurrency risk: same-resource writes may race, duplicate, conflict, or observe intermediate state; Hermes SHALL report actual per-call outcomes and SHALL not serialize MCP calls by policy.
- [x] 4.8 Verify no tool-call quota, operation-class filter, per-server denylist, or MCP-specific serialization gate is configured; document that Tool Search's `max_search_limit` is discovery-page size only, not a call limit.
- [ ] 4.9 Run offline `hermes -p default prompt-size --platform cli --json` and `--platform telegram --json` before and after the unchanged MCP inventory is active. Record total/tool-schema bytes and Tool Search tier/catalog mode without retaining private prompt contents or making an inference call.
- [ ] 4.10 In fresh CLI and Telegram sessions, use Tool Search to discover and describe representative current MCP Router read-only and mutating operations by exact `mcp__mcp_router__<tool>` name; verify underlying hooks still apply without changing MCP setup. Do not execute a shared-state mutation without separate approval.

## 5. Shared default profile and existing gateway

- [x] 5.1 Verify from official architecture, installed source, live profile inventory, gateway status, and session statistics that local CLI/Desktop do not require a gateway, `default` can serve CLI and Telegram concurrently, and source/chat-specific session records remain distinguishable in the shared database.
- [x] 5.2 Verify `default` remains the sole active profile and `gateway.multiplex_profiles` remains unset/false; do not create, clone, activate, or install a gateway for a named Telegram profile.
- [x] 5.3 Preserve explicit Telegram allowed-user authorization and the existing bot-token reference; do not enable global/platform allow-all users or copy credentials to another profile.
- [x] 5.4 Set `terminal.backend=local`, `terminal.home_mode=auto`, and `terminal.cwd=/Users/androidteam/Developer`; verify the directory exists and is not a Git repository. Each coding task must enter a verified target repository before Git; the cwd is not a filesystem sandbox.
- [x] 5.5 Record intentional sharing of default-profile config, credentials, SOUL, memory, skills, cron, MCP, logs, backups, and `state.db`, plus source/chat separation of active sessions and the accepted shared-state blast radius.
- [x] 5.6 Record the future split trigger: use a separate reviewed profile/gateway change only if identity, model/provider, credentials, tools, memory, skills, cron, users, or bot tokens must diverge.

## 6. Validation and activation

- [x] 6.1 Run default-profile `hermes config check`, `hermes doctor`, `hermes status --all`, CLI/Telegram tool inventories, MCP diagnostics, memory/skill status, cron status/history, permissions, and storage checks; record pass/fail without secrets.
- [ ] 6.2 Verify the exact effective target matrix in `design.md`, including empty deny/allow/disabled-toolset lists, subagent auto-approval, private URLs, browser evaluation, lazy installs, memory/skill writes, delegation, loop controls, compression, local backend, redaction, and user authorization.
- [x] 6.3 Verify intentional shared-state behavior: CLI/Desktop and Telegram use the same default config, credentials, memory, skills, cron, MCP, logs, backups, and `state.db`; active sessions remain attributable by source/chat/topic and no named Telegram profile exists.
- [ ] 6.4 With service-mutation approval, restart the existing default gateway in place. Verify launchd label `ai.hermes.gateway`, default profile home, one Telegram-token owner, a supervised PID, adapter health, filtered logs, cron ticker, and an authorized Telegram round-trip. Do not install, uninstall, multiplex, or transfer ownership to another gateway.
- [ ] 6.5 Run read-only smoke evidence first: effective settings, tool/MCP schemas, auth presence, deep gateway/platform health, hardline/static guard tests that execute no dangerous command, and synthetic-canary redaction through documented Hermes-managed surfaces.
- [ ] 6.6 With separately scoped approval, run local reversible-write tests for a disposable file, memory entry, skill, cron job, and nested delegation; verify results, then remove only the test artifacts through supported commands.
- [ ] 6.7 With separately scoped network/provider approval, test a harmless permitted private URL, browser JavaScript evaluation, model call, media capability, and authorized Telegram response. Confirm immutable metadata/link-local credential URLs remain blocked.
- [ ] 6.8 Do not execute MCP/shared-service mutation merely for smoke testing. If the user separately approves a specific reversible business mutation, name its target, expected effect, cleanup, and verification before calling it.
- [x] 6.9 Confirm Hermes still blocks immutable catastrophic commands, sudo-stdin guessing without configured support, protected credential-path file writes, and metadata/link-local credential endpoints; document these as exceptions to “all operations.”
- [ ] 6.10 If activation fails, restore prior default-profile configuration/state as needed and restart the same default gateway. If state restore is required, stop all Hermes processes and use classic CLI `/snapshot restore <id>` or reviewed `hermes import <archive>`; do not create or install a fallback profile gateway.
- [ ] 6.11 With reversible cron-write approval, create one disposable agent-backed job with explicit Telegram delivery, workdir, and provider/model policy plus one deterministic `no_agent` watchdog. Verify terminal run status, receipt, fail-closed model drift, and cleanup.
- [ ] 6.12 Verify long-turn/recovery behavior without corrupting live state: periodic progress, timeout warning, stale-interruption freshness, bounded startup-restore gate, and enabled delivery ledger. If a natural interruption produces a pending obligation, verify bounded replay and duplicate marker; never edit the ledger database manually.

## 7. Completion evidence

- [ ] 7.1 Record final effective settings, full tool/MCP inventories, unavailable prerequisites, diagnostics, shared default-profile topology, gateway status, backup metadata, smoke-test results, and residual warnings in a redacted implementation-evidence artifact.
- [x] 7.2 Document recurring maintenance: update check, schema migration, quick/full backup choice, tool/MCP inventory drift, credential rotation, gateway validation, and rollback.
- [x] 7.3 Record `hermes insights`, session statistics, cron history, and offline prompt-size trends after a representative workload. Treat community cost reports as risk signals, not measured local facts; retain unlimited Hermes budgets unless a separate approved provider/proxy budget change is introduced.
- [ ] 7.4 Re-run strict OpenSpec validation and verify every task is completed with evidence or explicitly blocked by a named package/credential/service prerequisite; do not archive, commit, push, or publish without separate authorization.
