# jira-mr-only-comments — Delta Specification

## ADDED Requirements

### Requirement: Jira integration SHALL only post comments on MR-linked Jira issues

The jira-mr-only-comments contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for jira-mr-only-comments work.

#### Scenario: jira-mr-only-comments is implemented per the contract above

The jira-mr-only-comments runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
