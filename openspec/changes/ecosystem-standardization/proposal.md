## Why

The agent ecosystem (agent-core, agent-docs-sync, agent-harness) has accumulated inconsistencies from parallel development. This change addresses the genuinely outstanding gaps after validating the current state.

## Validated Gap Audit (Aug 2026)

### Already Closed (no action needed)

| Gap | Validation | Status |
|-----|-----------|--------|
| Ruff version alignment | All 16 repos pin `ruff>=0.16.1` | Closed |
| mypy version alignment | All 16 repos pin `mypy>=2.3.0` | Closed |
| pytest version alignment | All 16 repos pin `pytest>=9.1.1` | Closed |
| Coverage config alignment | All 3 repos have `[tool.coverage.run]` in pyproject.toml | Closed |
| Typer version alignment | All 3 repos pin `typer>=0.25.1` | Closed |
| docs-sync `[tool.coverage]` | Present at lines 98-100 | Closed |
| graphify-out/ tracking | Intentionally tracked per .gitignore comments; contains graph.json, reports, labels | Closed (not a gap) |

### Remaining Gaps

| Gap | Severity | Repo | Action |
|-----|----------|------|--------|
| `src/reports-out/` build artifacts | Medium | agent-core | Remove 324 tracked daily report files (`blocked-*.md`, `wip-age-*.md`), add to .gitignore |

## What Changes

- **Clean build artifacts**: Remove `src/reports-out/` (324 files) from agent-core and add `src/reports-out/` to `.gitignore`
- **Refresh documentation counts**: Update README, AGENTS.md, and SPEC_INDEX files with accurate test/import/symbol counts
- **Clean OpenSpec store**: Address 2 unrelated failing changes (`align-jti-skill-runtime-contract`, `integrate-fable-5`) and remove untracked `doc-sync/SKILL.md` scaffold

## What Does NOT Change

- Dependency versions (already aligned)
- `graphify-out/` directories (intentionally tracked)
- CLAUDE.md / AGENTS.md (auto-generated by GitNexus or repo-specific; no duplication)
- Runtime behavior (tooling/documentation only)
- Docker/PostgreSQL scheduler smoke tests

## Capabilities

### New Capabilities
None — this is a cleanup/standardization change with no new behavioral capabilities.

### Modified Capabilities
None — no spec-level requirements are changing.

**Note**: This change sets `skip_specs: true` because no behavioral requirements change.

## Impact

**Affected Repositories:**
- `agent-core` — build artifact removal
- OpenSpec store — 2 unrelated failing changes resolved

**Risk:** Low — all changes are internal tooling/documentation improvements with no runtime behavior impact.
