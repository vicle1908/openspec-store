## 1. Establish the store-owned planning and evidence baseline

- [x] 1.1 Run `openspec status --change complete-agent-llm-config-integration --json --store openspec-store`; retain the reported schema, concrete artifact paths, action context, `linkedContext`, and `allowedEditRoots`, and verify the store-owned workflow grants no implementation-repository write authority.
- [x] 1.2 Declare `/Users/androidteam/Developer/openspec-store/openspec/changes/complete-agent-llm-config-integration/evidence/` as the only retained evidence root and define a versioned credential-safe handoff and live-row schema contract covering planning-baseline/current-store identity, concrete artifact and schema identities, repository/worktree identity, dependency declaration/path/origin/SHA, product/test/script/generated dirt, commands, exits, counts, impact/change-scope results, rollback, prerequisites, gates, and blockers.
- [x] 1.3 Inventory the complete `openspec-store` status and classify every path as corrective-change-owned or externally owned; verify unrelated active changes, OMP archives/specs, reports, generated state, and untracked paths will neither be staged, removed, repaired, synchronized, nor archived through this change.
- [x] 1.4 Record the full pre-baseline store SHA and immutable tree identities for archived `standardize-agent-llm-environment-resolution-v2` and `integrate-canonical-cli-projections-v1`; verify neither predecessor archive changes during planning, implementation, synchronization, rollback, or archive preparation.
- [x] 1.5 Inventory the identities and declared ownership of concurrent credential-loading, shell-activation, provider-launcher, and native-runtime changes; verify this change owns none of their paths and define their presence-only state as an external prerequisite whose drift can block or stale live evidence.
- [x] 1.6 Run focused strict validation, full-store strict validation, parsed-delta inspection, proposal-to-delta capability comparison, scenario counts, task-numbering/count checks, trailing-whitespace inspection, and `git diff --check`; retain exact commands, exits, and totals as planning evidence while separately attributing any unrelated full-store failure.
- [x] 1.7 Record the commit that first introduced the complete corrective planning subtree, the current corrective subtree tree identity, current branch divergence, and every path in that introducing commit; classify its mixed-purpose paths without rewriting, amending, rebasing, splitting, or otherwise repairing history, and verify no unchanged corrective file is restaged merely to manufacture a dedicated baseline.
- [x] 1.8 After separate store-commit authorization, stage only the approved corrective planning-reconciliation diff, inspect `git diff --cached --name-status` and cached content, verify no unrelated path, canonical spec, predecessor archive, generated file, or protected value is staged, commit the reconciliation separately, and record the resulting full store SHA plus corrective subtree identity; keep implementation evidence, canonical-spec synchronization, and archive movement in later separately reviewable lifecycle commits.

## 2. Freeze repository identities and issue one-repository work packets

- [ ] 2.1 Read the closest `AGENTS.md` and owning documentation for `tdt-core`; record full main/candidate SHAs, merge base, ancestry, branch/upstream, worktrees, complete product/test/script/generated dirt, toolchain, impact command, focused/full gates, Graphify policy, change-scope command, dependency consumers, and pre-integration rollback point.
- [ ] 2.2 Read the closest `AGENTS.md` and owning documentation for `agent-core`; record full main/candidate identities, actual `tdt-core` dependency declaration/path/origin/SHA, ownership, dirt, construction entry points, impact and verification commands, Graphify/change-scope policy, and rollback point.
- [ ] 2.3 Read the closest `AGENTS.md` and owning documentation for `agent-docs-sync`; record full main/candidate identities, actual upstream dependency declarations/paths/origins/SHAs, ownership, dirt, workflow persistence/resume paths, impact and verification commands, Graphify/change-scope policy, and rollback point.
- [ ] 2.4 Read the closest `AGENTS.md` and owning documentation for `agent-harness`; record full main/candidate identities, actual dependency origins, ownership of any duplicate production-composition edit, complete dirt, impact and verification commands, Graphify/change-scope policy, and rollback point.
- [ ] 2.5 Read the closest `AGENTS.md` and owning documentation for `ai-harness-skills`; record full main/candidate identities, editable/locked `tdt-core` declaration, actual import origin/SHA, frozen quality gates, RED/GREEN commit rule, Graphify policy, complete product/test/script/generated dirt, and rollback point.
- [ ] 2.6 Read the closest `AGENTS.md` and owning documentation for `ai-review`; record full main/candidate identities, editable/locked `tdt-core` declaration, actual import origin/SHA, projection and live-script ownership, quality gates, Graphify policy, complete product/test/script/generated dirt, and rollback point.
- [ ] 2.7 Define the dependency-ordered execution topology and proposed one-writer/one-dedicated-worktree mapping without creating, pruning, cleaning, resetting, or otherwise changing worktrees; classify each known candidate as selective source intake, excluded empty or main-equivalent state, or preserved missing/prunable historical state; mark prior candidate tests stale for integration acceptance; and stop packet issuance on ownership overlap, unresolved provenance, unexpected dependency origin, or unexplained product/test/script dirt.
- [ ] 2.8 Create a blocked `tdt-core` packet bound to the approved planning-baseline store SHA and corrective subtree identity and limited to atomic `agent-config-resolution`, exact `provider-model-profile-resolution`, canonical CLI selection, workspace-wide protected-credential caller/export/example migration, owned tests, and repository-required verification paths; identify selectively reusable candidate source without accepting its prior tests or dependency environment, and require accepted validator task 2A plus a current `handoff_acceptance` preflight before its handoff can be accepted.
- [ ] 2.9 Create a blocked `agent-core` packet limited to `agent-core-model-resolution`, every public construction entry point, missing-snapshot pre-discovery failure, explicit-model zero-read behavior, concurrent snapshot isolation, owned tests, and repository-required gates; bind the accepted `tdt-core` identity before unblocking it.
- [ ] 2.10 Create a blocked `agent-docs-sync` packet limited to canonical operation context, runtime-control projection, stable public provenance, retry reuse, resume identity, nested-result normalization, pre-side-effect failure, owned tests, and repository-required gates; bind accepted upstream identities before unblocking it.
- [ ] 2.11 Create a blocked `agent-harness` packet limited to the existing runner-contract effective-model propagation and containment-before-write correction, owned tests, and repository-required gates; bind accepted upstream identities before unblocking it.
- [ ] 2.12 Create a blocked `ai-harness-skills` packet limited to its native CLI adapter, preservation of CLI/canonical-provider identities, genuine-absence fallback, canonical-error no-launch behavior, actual dependency origin, required RED/GREEN commit shape, and owned product/test paths.
- [ ] 2.13 Create a blocked `ai-review` packet limited to its reviewer adapter, preservation of CLI/canonical-provider identities, genuine-absence fallback, canonical-error no-launch behavior, actual dependency origin, live-script disposition, and owned product/test/script paths.
- [ ] 2.14 Obtain explicit `tdt-core` repository-scoped implementation authority before task 3.1; only after authority, create or designate its dedicated worktree and sole writer, capture the starting SHA, branch, ancestry, complete dirt, and rollback point, and otherwise leave its packet blocked without treating this plan, parser readiness, candidate source, or a workset as permission.
- [ ] 2.15 Obtain explicit `agent-core` repository-scoped implementation authority before task 4.1; only after authority, create or designate its dedicated worktree and sole writer, capture its starting identity and complete dirt, and keep the packet dependency-blocked until the accepted `tdt-core` declaration, path, import origin, and full SHA are bound; otherwise leave it blocked without treating this plan, candidate source, parser readiness, or a workset as permission.
- [ ] 2.16 Obtain explicit `agent-docs-sync` repository-scoped implementation authority before task 5.1; only after authority, create or designate its dedicated worktree and sole writer, capture its starting identity and complete dirt, and keep the packet dependency-blocked until accepted `tdt-core` and `agent-core` declarations, paths, import origins, and full SHAs are bound; otherwise leave it blocked without treating this plan, candidate source, parser readiness, or a workset as permission.
- [ ] 2.17 Obtain explicit `agent-harness` repository-scoped implementation authority before task 6.1; only after authority, create or designate its dedicated worktree and sole writer, capture its starting identity and complete dirt, and keep the packet dependency-blocked until accepted upstream declarations, paths, import origins, and full SHAs are bound; otherwise leave it blocked without treating the one-line candidate, this plan, parser readiness, or a workset as permission.
- [ ] 2.18 Obtain explicit `ai-harness-skills` repository-scoped implementation authority before task 7.1; only after authority, create or designate its dedicated worktree and sole writer, capture its starting identity and complete dirt, preserve the required RED/GREEN commit shape, and keep the packet dependency-blocked until its declaration, lock/editable path, import origin, and full SHA resolve accepted `tdt-core`; otherwise leave it blocked without treating this plan, existing bridge tests, parser readiness, or a workset as permission.
- [ ] 2.19 Obtain explicit `ai-review` repository-scoped implementation authority before task 8.1; only after authority, create or designate its dedicated worktree and sole writer, capture its starting identity and complete product/test/script dirt, and keep the packet dependency-blocked until its actual `tdt-core` and `agent-core` declarations, paths, import origins, and full SHAs resolve accepted identities; otherwise leave it blocked without treating passing legacy tests, the live script, this plan, parser readiness, or a workset as permission.

## 2A. Implement and accept the store-owned evidence validator

- [ ] 2A.1 Obtain explicit `openspec-store` script/test implementation authority only; designate one dedicated worktree and sole store writer, bind the packet to the approved planning-baseline SHA and corrective subtree identity, inventory all existing store dirt, and limit ownership to `scripts/validate-agent-llm-evidence.py`, `tests/test_validate_agent_llm_evidence.py`, `tests/fixtures/agent-llm-evidence/`, and `openspec/changes/complete-agent-llm-config-integration/evidence/schema/v1/evidence-record.schema.json` without modifying planning artifacts, product repositories, canonical specs, predecessor archives, credentials, or unrelated paths; retain commit as a separate authorization gate in task 2A.13.
- [ ] 2A.2 Specify and fixture the versioned evidence-record and explicit repository-map contract for deterministic handoffs and live rows, all eight selected gates, planning-baseline ancestry plus observed current store `HEAD`, concrete `artifactPaths.<id>.existingOutputPaths`, complete relevant porcelain dirt, dependency declarations/locks/editable sources/filesystem checkouts/installed origins/full SHAs, non-secret source and mechanism identities, presence-only prerequisites, record dependencies, and explicitly selected result output.
- [ ] 2A.3 Add and run RED subprocess fixtures for a fully current deterministic record and a fully current live-row record; require canonical JSON, stable comparison ordering, exact record/gate attribution, no default wall-clock field, no credential value, and exit `0` only when every gate-required field is current.
- [ ] 2A.4 Add and run RED fixtures for proposal, each concrete delta spec, design, tasks, evidence-schema, planning-baseline ancestry, and corrective-tree drift; prove a descendant evidence-only store commit does not self-invalidate unchanged planning/schema identities while a missing/non-ancestor baseline or concrete planning/schema drift returns exit `2`, marks affected records `stale`, and invalidates all downstream evidence.
- [ ] 2A.5 Add and run RED fixtures for repository/worktree SHA drift, branch/worktree mismatch, tracked and untracked product/test/acceptance-script/generated dirt drift, missing or unclassified relevant dirt, and result-output ledger changes; prove complete dirt is compared, only explicitly classified non-secret content is fingerprinted, and selected result output cannot invalidate itself merely by being written.
- [ ] 2A.6 Add and run RED fixtures for `pyproject.toml`, `uv.lock`, local/editable source, filesystem checkout, installed module origin discovered without importing the target package, and full dependency-SHA agreement; cover stale lock/editable origin, stale wheel/cache/candidate checkout, missing dependency, inaccessible origin, and conflicting declaration/origin identities.
- [ ] 2A.7 Add and run RED fixtures for malformed schema/record/repository map, internally contradictory identity declarations, missing required fields, inaccessible local sources, and indeterminate presence-only prerequisites; require deterministic precedence `invalid` over `blocked` over `stale` over `current` and exits `4`, `3`, `2`, and `0` respectively.
- [ ] 2A.8 Add and run RED dependency-propagation fixtures proving planning/store/schema drift invalidates all records; actual resolved `tdt-core` drift invalidates every actual dependent consumer; actual resolved `agent-core` drift invalidates docs-sync, harness, and `ai-review` only where its recorded environment resolves it; consumer-local drift invalidates only that consumer evidence/live row; and live mechanism/prerequisite drift invalidates only the affected live row without manufacturing an edge from the planning graph alone.
- [ ] 2A.9 Add and run RED credential-canary, redaction, and no-side-effect fixtures proving the validator reads only environment-key names and presence booleans, never reads/prints/compares/hashes/serializes a credential value, never calls a provider or consumer, never imports a target package for origin discovery, never resolves dependencies over the network, and never cleans, resets, checks out, prunes, stages, commits, synchronizes, archives, writes a lockfile, or mutates a repository/worktree except for one explicitly selected result output.
- [ ] 2A.10 Implement the standard-library validator with explicit local inputs, schema validation, authoritative OpenSpec/Git/dependency/source/mechanism/prerequisite recapture, safe installed-origin discovery, complete NUL-delimited porcelain handling, gate-required field selection, per-field comparison, actual-origin dependency edges, downstream invalidation, and read-only behavior by default.
- [ ] 2A.11 Implement canonical JSON and stable exits `0`/`2`/`3`/`4`, including exact expected/current/source/decision fields, affected records, downstream decisions, credential-safe diagnostics, optional `observed_at` metadata excluded from comparison digests, and output-path containment that permits only the caller-selected result file.
- [ ] 2A.12 Run the full store-local validator fixture suite and source/style checks; rerun focused and full-store strict OpenSpec validation, task/scenario/count inspection, credential-canary scans, `git diff --check`, current GitNexus change-scope detection, and direct path-level review; record Graphify index availability and, only if a real `graphify-out/graph.json` exists, run the required query/update without treating absent Graphify state as a passed graph gate; retain exact commands, exits, counts, and complete dirt, and stop on an unexpected path, protected value, network/provider/consumer access, mutation, or unavailable required GitNexus gate.
- [ ] 2A.13 After separate store-script/test commit authorization, stage only the validator, schema, tests, and non-secret fixtures; inspect cached name/content diffs, run the required current GitNexus `detect_changes` scope check, commit separately from planning and evidence, record the pre/post SHAs and normal revert point, and verify unrelated dirty paths remain byte-for-byte preserved; leave commit blocked if the required graph gate is unavailable or reports unexpected scope.
- [ ] 2A.14 Run the committed validator against a schema-valid baseline record and the accepted planning/validator identities at `handoff_acceptance`; retain canonical output showing `current` with exit `0`, prove a validator result is evidence currency rather than implementation/live acceptance or lifecycle authorization, and leave every repository handoff blocked until its own applicable preflight succeeds.

## 3. Execute and accept the `tdt-core` work packet

- [x] 3.1 In the separately authorized `tdt-core` worktree, run current upstream impact analysis for each symbol that may change; retain callers and affected flows, pause on repository-defined HIGH/CRITICAL risk, and report any bounded manual fallback without representing it as a passed graph gate.
- [x] 3.2 Audit all protected credential resolve/reveal callers, raw environment-key callers, package exports, tests, examples, scripts, and participating workspace repositories before changing the provider-bound API; classify every site as migrated, unaffected, absent, or blocked.
- [x] 3.3 Intake only packet-owned source and tests; verify the worktree diff contains no unrelated candidate path, predecessor artifact, external credential/shell/provider path, protected value, or generated/index cleanup.
- [x] 3.4 Add and run RED atomic-capture tests covering agent identity, effective root/profile, YAML/dotenv/loader identities, non-secret fingerprints, overlay policy, detached explicit overrides, relevant registered environment values, redacted credential availability/provider binding, unregistered-environment stability, post-capture mutations, next-request refresh, complete cache isolation, and simultaneous-resolution isolation.
- [x] 3.5 Add and run RED compatibility tests covering typed/mapping parity, merged result, explicit path precedence, idempotence, unknown-agent behavior, strict and expanded allowed-key policies, domain-key exclusion, complete effective cache identity, and fail-closed validation of every malformed, unknown, ambiguous, or mismatched primary and fallback.
- [x] 3.6 Add and run RED protected-credential tests covering explicit non-empty provider ID, typed and raw-key references, exactly-one metadata matching, missing/duplicate/ambiguous/unbound/mismatched/cross-provider relationships, another-provider availability, pre-lookup failure, secret-safe fingerprints, and non-serialization/redaction.
- [x] 3.7 Add and run RED CLI-selection tests covering separate CLI adapter and canonical provider identities, executable/capability versus credential ownership, valid projection, genuine absence, legacy-only absence, unreadable source, resolution/projection failure, missing or ambiguous model selection, and zero fallback/launch on canonical error.
- [x] 3.8 Implement one immutable captured-input transaction, detach mutable overrides at capture, capture registered environment state once, derive typed and compatibility projections from that capture, include the complete effective non-secret identity in cache reuse, and prevent cross-request mutable global contamination.
- [x] 3.9 Implement the explicit provider-bound protected-value sequence, require exactly one matching metadata relationship before environment lookup, preserve provider-local new-schema `auth_env` and legacy registry authority, and migrate every caller/export/example classified in task 3.2.
- [x] 3.10 Implement typed CLI outcomes for selected, genuine mapping absence, and canonical error; preserve both identities and redacted provenance/provider-binding metadata in provider-neutral projections without inferring either identity.
- [x] 3.11 Run focused GREEN capture, compatibility, cache, concurrency, credential, selector, source-mutation, and redaction tests plus static conformance checks; retain exact commands, exits, counts, and prerequisites.
- [x] 3.12 Run the full repository suite and all required lint/type/package gates with isolated caches and a disposable non-secret `TDT_HOME`; retain exact commands, exits, pass/skip/fail counts, and dependency-consumer disposition.
- [x] 3.13 Run the repository-required Graphify update, inventory generated changes separately, and verify no unrelated generated/index state is removed or represented as product work.
- [x] 3.14 Run the repository change-scope gate, inspect `git diff --check` and every product/test/script/generated path, and stop before commit if affected paths, symbols, or flows exceed the packet.
- [x] 3.15 Commit only the accepted `tdt-core` correction in its repository, record full and pre-integration SHAs plus rollback point, and produce a credential-safe handoff without editing the shared store ledger.
- [x] 3.16 As the sole store writer, run the committed validator for `handoff_acceptance` and `task_completion`, require `current` with exit `0`, verify the handoff against current source, exact SHA, complete dirt, commands, impact/change-scope results, all owned scenarios, and resolved prerequisite state, and retain it under the absolute evidence root before marking tasks 3.1–3.15 complete.

## 4. Execute and accept the `agent-core` work packet

- [x] 4.1 In the separately authorized `agent-core` worktree, run `downstream_unblock` preflight and require `current` with exit `0`, prove the declared dependency path, import origin, and full SHA resolve the accepted `tdt-core`, run current upstream impact analysis for every construction symbol, and pause on repository-defined HIGH/CRITICAL risk.
- [x] 4.2 Intake only packet-owned source/tests and enumerate every public model constructor, SDK builder, CLI helper, base-agent path, fallback-chain builder, runtime helper, configuration read, environment read, credential lookup, and model-discovery surface.
- [x] 4.3 Add and run RED tests proving identifier-based SDK, CLI, base-agent, fallback-chain, and runtime-helper construction reuse the exact caller snapshot and preserve primary, fallback order, provider route, behavior, provenance, fingerprints, and binding metadata without a second resolution.
- [x] 4.4 Add and run RED tests proving every model-identifier path without a caller snapshot raises one consistent typed error before canonical/legacy configuration, YAML, dotenv, process environment, provider credentials, fallback catalogs, model registries, or other discovery.
- [x] 4.5 Add and run RED tests proving an explicit `Model` is returned unchanged, remains authoritative over conflicting selection fields, performs zero source/discovery reads, and remains isolated from simultaneous snapshot-based and explicit-model constructions.
- [x] 4.6 Implement the discriminated construction-input boundary; remove compatibility resolution from constructors, place optional one-time resolution only in a separate caller-owned operation/composition boundary, and thread the immutable snapshot through all nested builders without process-global state.
- [x] 4.7 Remove independent precedence/source reads from construction modules and run focused GREEN constructor, missing-snapshot, explicit-model, concurrency, and static source-conformance checks; retain exact commands, exits, counts, and accepted `tdt-core` identity.
- [x] 4.8 Run the full repository suite, Ruff, formatting, strict mypy, pre-commit/package gates, and `git diff --check` with isolated caches where required; retain exact results and complete dirt.
- [x] 4.9 Run the repository-required Graphify update, inventory generated state separately, run the change-scope gate, and stop before commit on an unexpected path, symbol, flow, or configuration read.
- [x] 4.10 Commit only the accepted `agent-core` correction, record full and pre-integration SHAs plus rollback point, and return a credential-safe handoff without editing the store ledger.
- [x] 4.11 As the sole store writer, run `handoff_acceptance` and `task_completion` preflight and require `current` with exit `0`, then verify and retain the handoff against source and dependency identities, missing-snapshot and zero-read evidence, concurrency isolation, owned scenarios, impact/change-scope results, gates, and dirt before marking tasks 4.1–4.10 complete.

## 5. Execute and accept the `agent-docs-sync` work packet

- [ ] 5.1 In the separately authorized docs-sync worktree, run `downstream_unblock` preflight and require `current` with exit `0`, prove the declared paths/import origins/full SHAs resolve accepted upstream dependencies, and run impact analysis across configuration, operation context, discovery, validation, generation, retry, resume, persistence, approval, reporting, and write-capable construction symbols.
- [ ] 5.2 Intake only packet-owned corrections and inventory every domain-config source, canonical-profile projection, runtime-control consumer, provenance boundary, retry path, checkpoint/resume field, model constructor, side-effect constructor, nested result, diagnostic, report, and CLI exit path.
- [ ] 5.3 Add and run RED precedence/schema tests covering explicit run override, registered docs-sync/shared environments, agent/global YAML, defaults, missing global config, invalid environment types, unknown/legacy sections, supported domain fields, and rejected repository LLM model/fallback/provider/behavior fields.
- [ ] 5.4 Add and run RED operation-projection tests proving effective timeout and iteration values remain identical across discovery, validation, generation, nested execution, diagnostics, normalized results, and reports; prove supported repository-domain runtime overrides do not mutate the source profile or authorize LLM fields.
- [ ] 5.5 Add and run RED spy tests proving malformed/non-mapping, secret-invalid, provider-invalid, fallback-invalid, unregistered, ambiguous, and cross-provider inputs fail before model construction, persistence initialization, approval advancement, or write-capable tool construction.
- [ ] 5.6 Add and run RED public-result tests proving typed provenance becomes one stable serializable redacted mapping and nested reports preserve distinct reachability, generation, completion, approval, compliance, provider error, usage, runtime controls, model diagnostics, and CLI exit outcomes.
- [ ] 5.7 Add and run RED retry/resume tests proving same-operation retries reuse the captured context, retained workflow state contains no credential values, valid resume restores matching non-secret identity, and absent/incomplete/mismatched/unrestorable identity fails before model, persistence, approval, or write-capable construction.
- [ ] 5.8 Implement one immutable docs-sync operation context containing canonical profile, domain-only `DocsSyncConfig`, effective runtime controls, public provenance mapping, non-secret resume identity, and approval/containment state; gate all side-effect-capable execution behind complete validation.
- [ ] 5.9 Implement same-operation retry reuse, credential-free retained state, exact resume identity validation, and nested-result normalization from the actual workflow result without successful zero-valued defaults or silent adoption of changed configuration.
- [ ] 5.10 Run focused GREEN precedence, runtime-control, domain-override, provenance, pre-side-effect, retry, resume, nested-result, report, and CLI tests; retain exact commands, exits, counts, and dependency identities.
- [ ] 5.11 Run the full repository lint/type/package/test gates, required Graphify update and change-scope inspection, `git diff --check`, and complete product/test/generated dirt review; stop on any path or flow outside the packet.
- [ ] 5.12 Commit only the accepted docs-sync correction, record full and pre-integration SHAs plus rollback point, and return a credential-safe handoff without editing the store ledger.
- [ ] 5.13 As the sole store writer, run `handoff_acceptance` and `task_completion` preflight and require `current` with exit `0`, then verify and retain the handoff against source/dependency identities, runtime/provenance/retry/resume evidence, pre-side-effect proof, owned scenarios, gates, and dirt before marking tasks 5.1–5.12 complete.

## 6. Execute and accept the `agent-harness` work packet

- [ ] 6.1 In the separately authorized harness worktree, run `downstream_unblock` preflight and require `current` with exit `0`, resolve ownership of duplicate production-composition edits, prove declared dependency paths/origins/full SHAs resolve accepted upstream repositories, and run impact analysis for production composition and containment symbols.
- [ ] 6.2 Intake one authoritative model-propagation correction without overwriting unrelated main, candidate, generated, or untracked state.
- [ ] 6.3 Add and run a RED production-composition test proving the already resolved effective `config.model` reaches service and agent construction without a harness-specific precedence path, configuration reread, or second resolution.
- [ ] 6.4 Add and run a RED containment test proving an invalid or unauthorized target fails before artifact-directory creation, approval advancement, or another write effect even when an effective model is configured.
- [ ] 6.5 Implement only effective-model propagation and preserve containment-before-write ordering under the existing canonical runner contract.
- [ ] 6.6 Run focused GREEN production-composition and containment tests plus the full repository quality gates; retain exact commands, exits, counts, dependency identities, and complete dirt.
- [ ] 6.7 Run the required Graphify update and change-scope gate, inventory generated state separately, and inspect `git diff --check` plus every product/test path before commit.
- [ ] 6.8 Commit only the accepted harness correction, record full and pre-integration SHAs plus rollback point, and return a credential-safe handoff without editing the store ledger.
- [ ] 6.9 As the sole store writer, run `handoff_acceptance` and `task_completion` preflight and require `current` with exit `0`, then verify and retain the handoff against exact identities, existing runner-contract behavior, containment proof, gates, and dirt before marking tasks 6.1–6.8 complete.

## 7. Execute and accept the `ai-harness-skills` work packet

- [x] 7.1 Unblock this separately authorized packet only after accepting final `tdt-core` and a `downstream_unblock` preflight returns `current` with exit `0`; prove dependency declaration, path, import origin, and full SHA resolve that identity, inventory product/test/script/generated dirt, and run impact analysis for selection, projection, and invocation symbols.
- [x] 7.2 Add and run the repository-required RED checkpoint covering preservation of CLI adapter and canonical provider IDs, valid canonical projection, genuine and legacy-only absence fallback, unreadable-source/resolution/projection no-fallback/no-launch behavior, provider-specific capabilities and arguments, redacted diagnostics, native-auth isolation, and no cross-provider metadata; commit RED separately.
- [x] 7.3 Update only adapter behavior proven divergent by RED tests, use CLI identity only for executable/capability decisions, use canonical provider ID only for credential metadata, preserve provider-native authentication, and reject unsupported generic behavior fields.
- [x] 7.4 Run focused GREEN runtime tests and commit the repository-required GREEN checkpoint separately from RED.
- [x] 7.5 Run frozen dependency sync, Ruff lint/format, strict mypy, full pytest/coverage, dependency audit, OpenSpec validation, skill validation, and `git diff --check`; retain exact commands, exits, counts, actual dependency origin/SHA, and dirt.
- [x] 7.6 Run `graphify update . --force`, inventory generated changes under repository policy, run the change-scope gate, and stop on an unexpected path, symbol, flow, dependency, or script change.
- [x] 7.7 Record the full accepted consumer SHA and commit series, pre-integration SHA, dependency provenance, rollback point, and complete dirt; return a credential-safe handoff without editing the store ledger.
- [x] 7.8 As the sole store writer, run `handoff_acceptance` and `task_completion` preflight and require `current` with exit `0`, then verify and retain the handoff against source/dependency identities, dual-identity and error/absence scenarios, commit shape, gates, and dirt before marking tasks 7.1–7.7 complete.

## 8. Execute and accept the `ai-review` work packet

- [ ] 8.1 Unblock this separately authorized packet only after accepting final `tdt-core` and a `downstream_unblock` preflight returns `current` with exit `0`; prove dependency declaration, path, import origin, and full SHA resolve that identity, resolve projection/test/live-script ownership, inventory complete dirt, and run impact analysis for reviewer construction and invocation.
- [ ] 8.2 Add and run RED reviewer tests covering preservation of CLI adapter and canonical provider IDs, valid per-provider projection, genuine and legacy-only absence fallback, unreadable-source/resolution/projection no-fallback/no-launch behavior, provider-specific arguments, redacted diagnostics, native-auth isolation, and no cross-provider model or credential metadata.
- [ ] 8.3 Update only reviewer behavior proven divergent by RED tests, use each identity only for its owning authority, preserve each native reviewer’s supported capability/authentication boundary, and do not absorb live-script or unrelated reviewer changes.
- [ ] 8.4 Run focused GREEN reviewer/projection tests and the full repository lint/type/source/test gates with isolated caches; retain exact commands, exits, counts, actual dependency origin/SHA, and complete dirt.
- [ ] 8.5 Run the required Graphify update and change-scope gate, inventory generated state separately, and inspect `git diff --check` plus every product/test/script path before commit.
- [ ] 8.6 Commit only accepted reviewer source/tests and separately disposition the live script; record full and pre-integration SHAs, dependency provenance, rollback point, and complete dirt before returning a credential-safe handoff.
- [ ] 8.7 As the sole store writer, run `handoff_acceptance` and `task_completion` preflight and require `current` with exit `0`, then verify and retain the handoff against exact source/dependency identities, dual-identity and error/absence scenarios, gates, script disposition, and dirt before marking tasks 8.1–8.6 complete.

## 9. Bind deterministic acceptance to exact integrated identities

- [ ] 9.1 Run the committed validator at `evidence_reuse` to recapture planning-baseline/current-store and concrete artifact/schema identities, all six repository/worktree SHAs, branch/upstream state, dependency declaration/path/lock/origin/SHA, complete product/test/acceptance-script/generated dirt, predecessor archive trees, canonical source identities, loader identity, and external prerequisite state immediately before deterministic acceptance; require `current` with exit `0` for each record considered for reuse.
- [ ] 9.2 Reuse a repository handoff only when `evidence_reuse` preflight returns `current` with exit `0` and planning identity, repository SHA, dependency provenance, relevant dirt fingerprint, product/test/script identity, command environment, canonical sources, loader behavior, and prerequisites exactly match task 9.1; otherwise retain `stale`, `blocked`, or `invalid`, propagate downstream invalidation, and rerun affected gates.
- [ ] 9.3 For `tdt-core`, retain an exact-bound reuse decision or rerun covering atomic capture, detached overrides, registered/unregistered environment behavior, secret-safe fingerprints, complete cache identity, concurrent isolation, compatibility parity, provider binding, CLI outcomes, and full gates.
- [ ] 9.4 For `agent-core`, retain an exact-bound reuse decision or rerun covering actual dependency origin, caller-snapshot reuse, missing-snapshot pre-discovery failure, explicit-model zero reads and authority, concurrent isolation, static conformance, and full gates.
- [ ] 9.5 For docs-sync, retain an exact-bound reuse decision or rerun covering actual dependency origins, effective runtime controls, domain overrides, stable public provenance, retry reuse, resume identity, pre-side-effect failure, nested results, and full gates.
- [ ] 9.6 For the harness, retain an exact-bound reuse decision or rerun covering actual dependency origins, effective-model propagation, containment-before-write, and full gates.
- [ ] 9.7 For `ai-harness-skills`, retain an exact-bound reuse decision or rerun covering actual dependency origin, dual identities, absence versus canonical error, provider arguments, native authentication ownership, product/test/script dirt, and full gates.
- [ ] 9.8 For `ai-review`, retain an exact-bound reuse decision or rerun covering actual dependency origin, dual identities, absence versus canonical error, reviewer projection/isolation, product/test/script dirt, and full gates.
- [ ] 9.9 Prove every consumer’s installed/editable declaration, filesystem path, import origin, and full SHA resolve accepted upstream identities rather than a stale worktree, wheel, lock entry, cache, or candidate checkout; retain only credential-free resolution output.
- [ ] 9.10 Run the workspace-wide static audit for independent configuration rereads, incomplete cache identity, secret-derived fingerprints, serialized protected values, credential literals, optional/missing provider context, raw-key binding bypass, identity inference, cross-provider substitution, unredacted diagnostics, invalid resume advancement, and reversed consumer dependencies; block acceptance until every owned finding is resolved and affected evidence recaptured.

## 10. Prove disposable precedence, isolation, and fail-closed security

- [ ] 10.1 Build a disposable `TDT_HOME` using unique non-secret sentinels for global and agent YAML, selected dotenv/loader identities, registered shared/docs-sync environments, unregistered environment controls, explicit source, detached run override, provider binding, and credential availability; record fixture structure without reading canonical credential values.
- [ ] 10.2 Run the precedence matrix and prove typed profile, compatibility mapping, caller-owned agent-core construction input, and docs-sync operation projection agree on primary, fallbacks, provider route, behavior, runtime controls, provenance, source identities, and non-secret fingerprints.
- [ ] 10.3 Mutate source files, registered and unregistered environment values, loader identity, and the original explicit-override object after capture; run simultaneous differently configured resolutions; prove in-flight isolation, detached overrides, registered-only effective identity, complete cache separation, and next-request refresh.
- [ ] 10.4 Probe malformed/non-mapping/unreadable files, invalid environment types, illegal repository LLM fields, malformed or unknown primary/fallback selections, missing/ambiguous CLI relationships, and projection errors; prove zero partial mapping, lower-priority substitution, local fallback, side-effect construction, or process launch.
- [ ] 10.5 Probe explicit matching, missing, duplicate, ambiguous, unbound, raw-key-only, typed-reference mismatch, cross-provider, and another-provider-available credential contexts; prove pre-lookup failure where required and scan fingerprints, caches, profiles, diagnostics, exceptions, workflow state, reports, and evidence for protected-value leakage.
- [ ] 10.6 Run credential-safe fixture cleanup and target-preservation checks, retain exact commands/exits, and verify no canonical credential, external authentication store, canonical `TDT_HOME`, unrelated target, or externally owned shell/provider path was copied, compared, rotated, deleted, or overwritten.

## 11. Rehearse repository-local rollback and invalidation

- [ ] 11.1 Rehearse normal commit-based `tdt-core` rollback in a disposable or dedicated worktree, run focused capture/credential/CLI gates at the restored identity, and record commands/exits without reset, clean, checkout, or unrelated-state mutation.
- [ ] 11.2 Rehearse normal commit-based `agent-core` rollback, run missing-snapshot/explicit-model/snapshot-isolation gates against restored dependencies, and record downstream evidence invalidation.
- [ ] 11.3 Rehearse normal commit-based docs-sync rollback, run runtime/provenance/retry/resume/pre-side-effect gates, prove a retained mismatched operation does not resume under changed identity, and record downstream invalidation.
- [ ] 11.4 Rehearse normal commit-based harness rollback, run focused composition/containment gates, and record downstream invalidation.
- [ ] 11.5 Rehearse normal commit-based `ai-harness-skills` rollback if accepted source changed, preserve its commit-series policy, run focused dual-identity/error-boundary gates, and record live-row invalidation.
- [ ] 11.6 Rehearse normal commit-based `ai-review` rollback if accepted source changed, run focused dual-identity/reviewer gates, and record live-row invalidation.
- [ ] 11.7 Run the committed validator over planning, upstream dependency, product/test/script, canonical-source, loader, launcher, shell/provider prerequisite, credential-availability/provider-binding, and rollback drift fixtures; prove affected deterministic and live evidence becomes `stale`, `blocked`, or `invalid` with the specified nonzero exit and downstream acceptance remains blocked until dependency-ordered recapture returns `current`.

## 12. Materialize and execute durable live acceptance

- [ ] 12.1 Define and validate `/Users/androidteam/Developer/openspec-store/openspec/changes/complete-agent-llm-config-integration/evidence/live-cli-matrix.json` with exactly two required independent rows—one `ai-harness-skills` contained generation row and one `ai-review` reviewer row—and no additional required row without explicit scope revision.
- [ ] 12.2 Select one currently supported and enabled provider per consumer, obtain explicit live-provider authorization for each concrete row, record external credential/shell/launcher ownership and prerequisite identity, run `live_authorization` preflight, and classify missing authorization, indeterminate identity, drift, or unavailable prerequisites as `blocked`, `invalid`, or `stale`, never skipped or passed.
- [ ] 12.3 Validate that each row binds planning SHA/tree, consumer SHA/worktree/dirt, dependency declaration/path/lock/origin/SHA, product/test/launcher/script identities, CLI adapter ID, canonical provider ID, source fingerprints, loader identity, provider-binding/availability metadata, prerequisites, contained operation, expected artifact, result path, and row state.
- [ ] 12.4 Perform presence-only authentication, executable, shell, provider, network, containment, dependency, and source checks without printing, comparing, copying, or modifying credentials; immediately before each row launch run `live_launch` preflight and require `current` with exit `0`, otherwise mark the row blocked, invalid, or stale before process launch.
- [ ] 12.5 Execute the authorized `ai-harness-skills` row through its true contained generation boundary and separately record process reachability/exit, actual nested result, expected nonce or artifact, target preservation, duration, redacted command shape, and row status.
- [ ] 12.6 Execute the authorized `ai-review` row through its true reviewer boundary and separately record process reachability/exit, actual nested result, expected nonce or artifact, target preservation, duration, redacted command shape, and row status.
- [ ] 12.7 Exercise ledger rejection for exit zero with provider error, incomplete nested result, missing artifact, target mutation, identity collapse, changed consumer/dependency/source/script identity, or changed shell/provider/credential prerequisite; prove rows become `failed` or `stale`.
- [ ] 12.8 Run credential/sentinel redaction checks over mechanisms, command captures, stdout/stderr, artifacts, matrix, and result records; classify scripts as reusable mechanisms only and exact-identity result records as evidence.
- [ ] 12.9 Run `evidence_reuse` preflight to recapture all invalidation fields, require `current` with exit `0` for both records, and verify both independent rows remain `passed`, neither is unauthorized/blocked/invalid/failed/stale, and no combined or aggregate result substitutes for either required consumer row before declaring live acceptance complete.

## 13. Verify all scenarios and reconcile the shared ledger

- [x] 13.1 Verify all 18 `agent-config-resolution` scenarios against accepted source and exact deterministic/disposable evidence, including atomic capture, detached overrides, registered environment state, secret-safe fingerprints, complete cache identity, compatibility validation, and concurrency; record every unsupported scenario as failed or blocked.
- [x] 13.2 Verify all 7 `provider-model-profile-resolution` scenarios against accepted source, workspace caller audit, pre-lookup provider-binding evidence, raw-key binding tests, and redaction/security evidence; record every unsupported scenario as failed or blocked.
- [x] 13.3 Verify all 7 `agent-core-model-resolution` scenarios against accepted source and exact caller-snapshot, missing-snapshot pre-discovery, explicit-model zero-read/authority, and concurrent-isolation evidence; record every unsupported scenario as failed or blocked.
- [x] 13.4 Verify all 25 `agent-docs-sync` scenarios against accepted source and exact precedence, runtime-control, repository-domain override, stable provenance, retry, resume, pre-side-effect, nested-result, report, and CLI evidence; record every unsupported scenario as failed or blocked.
- [x] 13.5 Verify the 10 selection and consumer-wiring scenarios in `cli-provider-profile-resolution` against accepted `tdt-core` and adapter source, dual-identity projections, actual dependency origins, genuine-absence fallback, canonical-error no-launch behavior, and provider-native invocation evidence.
- [x] 13.6 Verify the remaining 10 identity-bound live-evidence and automated-drift-validation scenarios in `cli-provider-profile-resolution` against the final two-row matrix, retained result records, validator fixtures, canonical JSON, exit codes, gate placement, credential/no-side-effect evidence, and dependency propagation; confirm tasks 13.1–13.6 cover exactly 77 delta scenarios and leave acceptance blocked unless both rows pass and every stale/blocked/invalid/current preflight probe works.
- [x] 13.7 Verify the existing canonical harness runner contract against accepted production-composition and containment evidence without inventing a sixth delta capability.
- [x] 13.8 Before checking each evidence-backed task, run `task_completion` preflight for its exact record and require `current` with exit `0`; reconcile it with the accepted handoff, committed source/dependency identities, complete dirt, exact commands/outputs, impact/change-scope results, rollback, prerequisite state, and integration status, and leave unchecked or blocked anything supported only by historical counts, uncommitted source, parser readiness, structural validation, or a non-current validator result.
- [x] 13.9 Recompute predecessor archive tree identities and compare them with task 1.4; inspect the corrective subtree and confirm it contains exactly the five authoritative delta capabilities plus owned proposal/design/tasks/evidence artifacts and no externally owned work.

## 14. Synchronize canonical specs and prepare archive handoff

- [x] 14.1 Run focused strict validation, full-store strict validation, parsed-delta inspection, proposal-to-delta capability comparison, exact 77-scenario count, task-count/completion validation, committed validator fixture tests, credential-safe evidence validation, direct whitespace checks, and `git diff --check`; report structural results separately from validator currency, implementation verification, and live acceptance.
- [x] 14.2 Resolve the authoritative synchronization inputs from current `artifactPaths.specs.existingOutputPaths` and verify they are exactly the five approved delta files; do not infer synchronization targets from proposal, design, tasks, archives, or evidence.
- [x] 14.3 Only after tasks 13.1–13.9 pass, both required live rows remain current and passed, and `spec_sync` preflight returns `current` with exit `0`, run the normal OpenSpec spec-sync workflow as the sole store writer under separate synchronization authorization.
- [x] 14.4 Inspect the canonical spec diff and prove each intended requirement/scenario appears exactly once, no unrelated main spec changes, no protected value appears, and neither predecessor archive changes.
- [x] 14.5 Run focused and full-store strict validation after synchronization and retain exact commands, exits, totals, and separately attributed external blockers.
- [x] 14.6 Stage only accepted ledger/evidence and the five synchronized canonical specs, inspect cached name/content diffs for unrelated paths or protected values, and commit synchronization/evidence separately from the planning baseline under separately granted store-commit authority.
- [x] 14.7 Record final store SHA, corrective tree identity, all six repository SHAs, actual dependency declarations/paths/origins/SHAs, complete dirt, rollback results, live-row outcomes, prerequisite state, validation totals, remaining blockers, and unrelated-path preservation proof.
- [x] 14.8 Run `archive_readiness` preflight and require `current` with exit `0`, then produce an archive-readiness handoff with the normal archive command but do not archive in this update/apply workflow; archive only through a later explicitly authorized OpenSpec archive workflow after all planning, source, dependency, prerequisite, evidence, synchronization, and store identities are recaptured and remain current.
