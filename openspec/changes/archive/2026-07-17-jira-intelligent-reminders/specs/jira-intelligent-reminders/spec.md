# jira-intelligent-reminders — Delta Specification

## ADDED Requirements

### Requirement: jira-daily-reports SHALL support policy-driven intelligent reminders

The jira-intelligent-reminders contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for jira-intelligent-reminders work.

#### Scenario: jira-intelligent-reminders is implemented per the contract above

The jira-intelligent-reminders runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
