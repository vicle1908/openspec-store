## 1. Fix Orchestration Docs

- [x] 1.1 Add subgraph section to `orchestration.md` (shared-state + wrapper patterns)
- [x] 1.2 Add Command API section to `orchestration.md` (CommandResult → Command)
- [x] 1.3 Add per-node features section to `orchestration.md` (retry, cache, error_handler, metadata, timeout)

## 2. Fix Streaming Docs

- [x] 2.1 Rewrite `streaming.md` to match actual `run_stream()` async generator API
- [x] 2.2 Remove outdated `stream_mode` LangGraph content

## 3. Add Harness Integration Docs

- [x] 3.1 Create `harness-integration.md` with overview of all 10 capabilities
- [x] 3.2 Add harness_config section to `configuration.md` with summary + link

## 4. Add Agent Config Docs

- [x] 4.1 Create `agent-config.md` for YAML/JSON agent loading via `load_agent_config()`

## 5. Update Extending Docs

- [x] 5.1 Add ToolSearch capability example to `extending.md`
- [x] 5.2 Add Thinking capability example to `extending.md`

## 6. Update README

- [x] 6.1 Add links for new docs to `README.md`

## 7. Verify

- [x] 7.1 Verify all new docs exist and are linked
- [x] 7.2 Verify no broken references
