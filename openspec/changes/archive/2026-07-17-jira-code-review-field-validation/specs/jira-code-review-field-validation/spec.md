# jira-code-review-field-validation — Delta Specification

## ADDED Requirements

### Requirement: Jira code-review field validation SHALL reject invalid field values

The jira-code-review-field-validation contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for jira-code-review-field-validation work.

#### Scenario: jira-code-review-field-validation is implemented per the contract above

The jira-code-review-field-validation runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
