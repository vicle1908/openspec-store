# Delta specs for collapse-feature-routing

This change modifies two existing capabilities (`code-daily-scan-core`
and `android-plugin`) and adds one new capability
(`single-source-of-truth-tab-routing`).

The original specs live in
`openspec/changes/unified-code-daily-scan/specs/`. We are not editing
those files in place; the OpenSpec archive process will reconcile the
deltas when the change is archived.
