# Design: Workspace Agent Cleanup

## Current State

### Agent Dot-Folders (go-microservices repo)

| Folder | Content | Tracked? | Action |
|---|---|---|---|
| `.agent/` | Empty `skills/` dir | No | **Remove** |
| `.agents/` | 36 skills (openspec×12, agentmemory×8, redis×7, graphify, iris-development, etc.) | 12 openspec files tracked | **Remove repo-level skills** — root workspace `~/.agents/` has 87 skills. Keep only if repo-specific |
| `.claude/` | CLAUDE.md, settings.json, settings.local.json, skills/generated/ (20), skills/gitnexus/ (6), skills/graphify/ (9) | Partial (some gitignored, some tracked) | **Keep config files** (settings.json hooks, settings.local.json permissions), **remove skills** (generated/gitnexus/graphify provided by root) |
| `.codex/` | hooks.json, AGENTS.md, skills/ (openspec×12 + graphify×9) | 12 openspec files tracked | **Keep hooks.json** (graphify hook-check), **remove skills** (openspec/graphify in root workspace) |
| `.cursor/` | Empty | No | **Remove** |
| `.factory/` | Empty | No | **Remove** |
| `.fable-5/` | 12 openspec skill files | Tracked | **Remove** — root workspace `~/.fable-5kills/` has these |
| `.kiro/` | Empty | No | **Remove** |
| `.omp/` | Empty | No | **Remove** |
| `.opencode/` | 61MB node_modules (single plugin dep) | Gitignored | **Remove entirely** — no skills used |
| `.pi/` | Empty | No | **Remove** |
| `.kilocode/` | Empty | No | **Remove** |

### Root Workspace (~/Developer/) — DO NOT TOUCH

| Folder | Status |
|---|---|
| `~/.agents/` | 87 skills — canonical |
| `~/.claude/` | 67 skills — symlinks to `~/.agents/skills/` |
| `~/.codex/` | 14 skills |
| `~/.fable-5kills/` | SKILL.md + 12 openspec skills |

### Stale Files to Remove

| File | Why Stale |
|---|---|
| `AGENTS.md.bak` | Backup file, `*.bak` in .gitignore |
| `.github/workflows/verify.yml.bak` | Backup file, `*.bak` in .gitignore |
| `tools/agentguide/validator.go.bak` | Backup file, `*.bak` in .gitignore |
| `agent-skills-manifest.json` | Generated by `scripts/agent_skill_surfaces.py`, 10 days stale, has duplicate entries |
| `skills-lock.json` | Locks skills from external repos (rohitg00/agentmemory, redis/agent-skills), not actively maintained |
| `services/.contract-check-*` | 6 temp worktree directories |

### .gitignore Fixes

1. Remove duplicate `.fable-5/` entry (listed twice)
2. Remove `.fable-5kills/graphify/` phantom path (directory doesn't exist)
3. Fix `.fable-5.json` reference if needed
4. Untrack files that are now gitignored via `git rm --cached`

### AGENTS.md Fix

Line 88 references `.fable-5kills/graphify/SKILL.md` — broken path.
Replace with correct reference to Hermes graphify skill.

## Execution Order

1. Remove empty agent dot-folders (safe, no files)
2. Remove repo-level skills (duplicates of root workspace)
3. Keep repo-specific config (hooks, permissions, CLAUDE.md)
4. Remove stale tracked files
5. Fix .gitignore
6. Fix AGENTS.md broken reference
7. Clean up temp contract-check dirs
8. Untrack files in gitignored directories
