# skill-split-compact — Delta Specification

## ADDED Requirements

### Requirement: Oversized SKILL.md files SHALL be split into focused sub-skill files

The skill-split-compact contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for skill-split-compact work.

#### Scenario: skill-split-compact is implemented per the contract above

The skill-split-compact runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
