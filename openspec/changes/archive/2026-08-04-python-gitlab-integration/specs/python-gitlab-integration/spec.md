# python-gitlab-integration — Delta Specification

## ADDED Requirements

### Requirement: Python projects SHALL use python-gitlab library for GitLab API access

The python-gitlab-integration contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for python-gitlab-integration work.

#### Scenario: python-gitlab-integration is implemented per the contract above

The python-gitlab-integration runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
