# use-native-cockpit-and-reassign-omp-model-roles

Route omp cockpit through its native OpenAI Responses endpoint instead of the Claude compatibility adapter, and reassign omp model roles across the three validated providers
