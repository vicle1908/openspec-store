# android-scan-mr-command — Delta Specification

## ADDED Requirements

### Requirement: code-daily-scan SHALL support an MR-scoped invocation mode

The android-scan-mr-command contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for android-scan-mr-command work.

#### Scenario: android-scan-mr-command is implemented per the contract above

The android-scan-mr-command runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
