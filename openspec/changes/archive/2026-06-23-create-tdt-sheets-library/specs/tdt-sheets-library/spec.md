# tdt-sheets-library — Delta Specification

## ADDED Requirements

### Requirement: tdt-sheets SHALL be the canonical Python client for Google Sheets operations

The tdt-sheets-library contract SHALL declare the delta for this change. The pre-existing capability surface is preserved; this delta is the canonical OpenSpec declaration. Operators SHALL follow this contract for tdt-sheets-library work.

#### Scenario: tdt-sheets-library is implemented per the contract above

The tdt-sheets-library runtime SHALL pass the contract above when the implementation is invoked through the supported entry points.
