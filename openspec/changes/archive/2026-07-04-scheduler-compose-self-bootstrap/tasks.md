# scheduler-compose-self-bootstrap — Tasks

> **Companion to:** `proposal.md` and `design.md` in this directory
> **Total tasks:** 24

---

## Task Group 1: Generic generator framework (in `agent-core`)

- [x] **1.1** Create `agent-core/deployments/scheduler/generators/__init__.py` exporting a `GENERATORS: dict[str, Callable[[], dict]]` registry.
- [x] **1.2** Move `generate_jira_manifest._main` body into `agent-core/deployments/scheduler/generators/jira.py` as a function `jira_manifest() -> dict`. The function takes no arguments; it reads cron constants from `jira_daily_reports.dbos_scheduling` directly via `inspect.getsource` parsing (the existing logic in `generate_jira_manifest.py:32-38`).
- [x] **1.3** Refactor `generate_jira_manifest.py` into `generate_schedule_manifest.py` with a `dispatch(repo: str, output_path: Path)` function that looks up the repo in `GENERATORS` and writes the result atomically.
- [x] **1.4** Add `agent-core/deployments/scheduler/generators/code_daily_scan.py` with `code_daily_scan_manifest() -> dict`. The function imports `code_daily_scan.config` from `/workspace/code-daily-scan/src`, calls `load_config("android")` and `load_config("ios")`, and projects to `tdt-schedule/v1`.
- [x] **1.5** Add `agent-core/deployments/scheduler/generators/tdt_observability.py` with `tdt_observability_manifest() -> dict`. The function imports `tdt_observability.retention` and reads `OBSERVABILITY_RETENTION_CRON` + `OBSERVABILITY_RETENTION_TZ` constants.

## Task Group 2: `tdt-observability` constant addition

- [x] **2.1** Add the following to `tdt-observability/src/tdt_observability/retention.py`:

  ```python
  OBSERVABILITY_RETENTION_CRON = "0 2 * * *"
  OBSERVABILITY_RETENTION_TZ = "UTC"
  ```

  Position them near `daily_observability_retention()`.

- [x] **2.2** Add a short comment block above the constants explaining they are the canonical source for the scheduler manifest. Operators tuning these must edit the source, not the manifest.

## Task Group 3: `entrypoint.sh` orchestration

- [x] **3.1** Rewrite `agent-core/deployments/scheduler/entrypoint.sh` to:
  - chown venv
  - mkdir schedules dir
  - For each repo in `jira-daily-reports code-daily-scan tdt-observability`, call `uv run python3 /generate_schedule_manifest.py <repo> /home/agent/.tdt/schedules/<repo>.yaml`
  - On any error, log and exit 1
  - Touch `.reload` atomically (tmp + rename)
  - exec `tdt-scheduler serve`

- [x] **3.2** Add `exec >> /home/agent/.tdt/logs/scheduler-entrypoint.log 2>&1` near the top so all entrypoint logs land in a host-visible file.
- [x] **3.3** Confirm `~/.tdt/logs` is created if missing (idempotent mkdir in entrypoint).

## Task Group 4: `compose.yaml` and Dockerfile adjustments

- [x] **4.1** Update `agent-core/Dockerfile` to include `ripgrep` in the `apt-get install` list. Add a comment explaining why (used by `code-daily-scan` for fast grep through Swift/Kotlin source). The full line becomes:
  ```dockerfile
  && apt-get install -y --no-install-recommends ca-certificates curl gcc git libpq-dev ripgrep tzdata \
  ```

- [x] **4.2** Update the Dockerfile COPY block to include `generate_schedule_manifest.py` and the `generators/` directory (alongside the existing `generate_jira_manifest.py` copy). Delete the legacy `generate_jira_manifest.py` from the COPY block (its code now lives in `generators/jira.py`).

- [x] **4.3** In `agent-core/compose.yaml`, ensure the `scheduler` service has `- ../tdt-observability/src` mounted (it already does at line 155 — verify and confirm via the smoke test).

## Task Group 5: `code-daily-scan/scripts/deploy.sh` reduction

- [x] **5.1** Remove the 60-line Python heredoc (lines 36-93 of current deploy.sh). Replace with:

  ```bash
  echo "=== note: code-daily-scan.yaml manifest is now generated by the scheduler container's entrypoint.sh ==="
  echo "=== touching .reload to trigger hot-reload of any config change ==="
  touch "$HOME/.tdt/schedules/.reload"
  ```

- [x] **5.2** Keep the rsync + uv sync + verify sections (lines 21-33) — they're about deploying the **app**, not the **manifest**. Add a comment clarifying the manifest section was removed.

## Task Group 6: Tests

- [x] **6.1** Create `agent-core/tests/scheduler/test_entrypoint_manifest_generation.py` with these tests:
  - `test_jira_generator_emits_valid_manifest` — runs `jira_manifest()`, validates against `tdt_core.scheduler.schedule_manifest.ScheduleManifest`.
  - `test_code_daily_scan_generator_emits_valid_manifest` — same, for android + ios. Requires `code_daily_scan` source to be on `sys.path`; fixture creates a temp `~/.tdt/code-daily-scan.yaml` with known cron values.
  - `test_tdt_observability_generator_emits_valid_manifest` — same, for the retention schedule.
  - `test_atomic_write_no_tmp_leak` — calls `dispatch(...)` with a path and asserts no `.tmp` file remains after success.
  - `test_invalid_repo_raises` — asserts unknown repo names raise `KeyError` (or a custom `UnknownManifestRepoError`).
  - `test_dispatch_does_not_touch_reload_sentinel` — asserts the function does NOT write to `~/.tdt/schedules/.reload`.

- [x] **6.2** Extend `agent-core/tests/test_docker_local_dev.py::test_dockerfile_matches_compose_versions` to assert `"ripgrep"` is in the `apt-get install` line. (Plus a new `test_scheduler_dockerfile_installs_ripgrep` that covers the scheduler-specific Dockerfile.)
- [x] **6.3** Create `agent-core/scripts/verify_scheduler_compose_up.sh` that:
  - Runs `docker compose down -v` (in the agent-core dir).
  - Runs `docker compose build scheduler` (forces fresh image build).
  - Runs `docker compose up -d scheduler`.
  - Waits up to 90s for `curl -fsS http://127.0.0.1:9100/scheduler/health` to return 200 + `dbos_connected=true`.
  - Asserts `health.reload.manifests_loaded == 3`.
  - Asserts `health.schedule_count >= 17` (current baseline; will adjust once new manifests add schedules).
  - Prints a clear PASS/FAIL summary.
  - On FAIL, runs `docker logs agent-core-local-scheduler-1 --since 5m` for context and exits 1.

## Task Group 7: docs

- [x] **7.1** Update `tdt-meta/docs/operations/schedule-registry.md` — add a section **"Manifest Bootstrap"** explaining that:
  - All `~/.tdt/schedules/*.yaml` are written by the scheduler container's `entrypoint.sh`.
  - The `code-daily-scan` manifest content is driven by the host's `~/.tdt/code-daily-scan.yaml` (via the in-container generator).
  - The `tdt-observability` manifest content is driven by source constants in `tdt_observability.retention`.
  - Hot-reload via `.reload` is still the canonical mechanism for runtime config changes.

- [x] **7.2** Update `tdt-observability/CLAUDE.md` (if present) and `tdt-observability/README.md` to note that the scheduler manifest is generated from source — operators should NOT hand-write `~/.tdt/schedules/tdt-observability.yaml`.
- [x] **7.3** Update `tdt-meta/docs/operations/scheduler-healthcheck.md` to add a note about the compose-up smoke test (`scripts/verify_scheduler_compose_up.sh`) being the canonical way to verify scheduler health from a fresh state. Keep the existing HTTP healthcheck guide as a separate, lightweight daily verification.
- [x] **7.4** Create `tdt-meta/docs/workflows/scheduler-compose-up-smoke.md` documenting:
  - When to run the smoke test (after any Dockerfile / compose.yaml / entrypoint.sh change).
  - What it asserts.
  - How to interpret failure (which log to read first).
  - The Linux-only CI caveat.

## Task Group 8: Validation & Verification

- [x] **8.1** Run `openspec validate scheduler-compose-self-bootstrap --strict --changes` and ensure exit code 0.
- [x] **8.2** Run `ruff check . --fix && ruff format .` inside `agent-core/` to confirm formatting/lint passes for new files.
- [x] **8.3** Run `mypy deployments/scheduler/ tests/scheduler/test_entrypoint_manifest_generation.py --strict` and confirm no new type errors are introduced.
- [x] **8.4** Run `pytest -x agent-core/tests/scheduler/test_entrypoint_manifest_generation.py -q` and confirm all tests pass.
- [ ] **8.5** Run `bash agent-core/scripts/verify_scheduler_compose_up.sh` end-to-end (on this macOS dev host) and confirm PASS.
- [x] **8.6** Verify the legacy `code-daily-scan/scripts/deploy.sh` no longer references `cd $HOME/deployments/code-daily-scan/app` in a misleading way; operator-facing notes updated.
- [ ] **8.7** Confirm via `tdt-scheduler schedules list` inside the freshly-built container that **all expected schedules are present** (cross-check against the current baseline of 18).

---

## Notes

- Tasks are ordered roughly by dependency: framework → constants → entrypoint → compose/Dockerfile → host-side deploy.sh reduction → tests → docs → validation.
- All test tasks (Group 6) should be implementable **before** the compose changes (Group 4) because they exercise the generators in isolation.
- Task 8.5 requires the host machine to have Docker available. If unavailable, Task 8.5 can be reported as `deferred` and the change still applied.
