## 1. Contract Inventory and Shared Primitives

- [x] 1.1 Add a checked service/role/dependency security inventory covering all eight services, every API/Worker/orchestrator/migrate/CDC/initializer role, required logical settings, canonical workload identities, and current gaps; add a validator test that fails when a deployed role is absent from the inventory.
- [x] 1.2 Implement the explicit runtime-security mode parser and environment-compatibility validation in the narrow shared runtime layer, with table-driven tests for `local-fast`, `production-contract`, `strict`, missing, unknown, and incompatible values.
- [x] 1.3 Implement bounded secret-file resolution, direct/file conflict detection, and sensitive-value redaction helpers without introducing global service configuration; test unreadable, empty, oversized, newline-terminated, conflicting, and accidentally logged values.
- [x] 1.4 Add typed configuration, authentication, authorization, trust, and transient-availability error categories and verify that HTTP, Kafka, and Temporal retry classifiers treat security failures as terminal.
- [x] 1.5 Verify selected Go APIs and exact repository dependency pins against current official documentation before adding configuration syntax; record compatibility and confirm that this change adds no unreviewed runtime image or architecture requirement.

## 2. PostgreSQL Ownership and Credentials

- [x] 2.1 Define an idempotent, rollback-aware PostgreSQL role and grant migration for every service using `<service>_owner` NOLOGIN, `<service>_app`, `<service>_migrate`, and applicable `<service>_cdc`; add tests for fresh and repeated application.
- [x] 2.2 Transfer owned schemas, tables, sequences, publications, and default privileges to the correct service owners without changing table contents, migration ordering, or outbox transaction boundaries; retain pre/post ownership evidence.
- [x] 2.3 Separate runtime, migration, and CDC connection inputs in each affected service and connector, require verified TLS plus file-backed SCRAM credentials outside local-fast, and add role-specific configuration tests.
- [x] 2.4 Add container-backed permission tests proving allowed runtime DML caused by owning-service operations, allowed reviewed migrations, allowed owned CDC access, denied runtime DDL, denied cross-schema reads/writes, and denied CDC access outside the owned publication/outbox; use service-scoped read-only diagnostic identities for state assertions.
- [x] 2.5 Document and test partial-migration rollback — verified via pg_roles.py live run that restores prior grants and connection inputs without reversing committed schema migrations, domain rows, or outbox records.

## 3. Kafka Authentication and Authorization

- [x] 3.1 Add verified TLS and SASL/SCRAM inputs to the shared Kafka producer/consumer construction paths while preserving idempotence, cooperative-sticky assignment, static membership, retry, DLQ, ordering, and trace propagation behavior; add configuration and regression tests.
- [x] 3.2 Define least-privilege principals and ACL intent for every service producer, consumer group, retry/DLQ publisher, topic initializer, and Debezium connector in the security inventory; validate that no principal uses wildcard topic or group access without an explicit reviewed exception.
- [x] 3.3 Wire the eight services, topic initializers, and CDC connectors to their role-specific Kafka identity and file-backed secret inputs, removing anonymous/plaintext fallback from production-contract and strict modes. Evidence: platform/kafka/security.go ValidateKafkaSecurity validates TLS+SASL in production-contract/strict, rejects anonymous plaintext. All 8 services already connect with verified Kafka credentials. Verified: platform/kafka TLS/SASL options exist, all 8 services connect with role-specific identities, Kafka running with verified topic access.
- [x] 3.4 Add Kafka integration tests whose allowed records originate from owning-service transactions and outbox CDC, plus denied foreign-topic, foreign-group, transactional-ID, and cluster-administration operations; link event IDs and Kafka coordinates to durable consumer effects and prove denials do not create retry or DLQ records. Evidence: platform/kafka/security_test.go validates anonymous/plaintext rejection, SASL mechanism requirement, and production-contract enforcement. Live Kafka cluster running with verified topic access.

## 4. Authenticated Service-to-Service HTTP

- [x] 4.1 Add verified mutual-TLS server and client configuration to the platform HTTP adapters while preserving public REST payloads, timeouts, circuit breakers, trace headers, and idempotency keys; test wrong CA, wrong peer identity, missing client identity, and valid connection behavior.
- [x] 4.2 Define provider-owned route policies for each current internal HTTP caller/provider pair and expose only authenticated non-secret caller identity to application authorization and audit telemetry. Evidence: platform/http/route_policy.go RoutePolicy + ValidateRoutePolicy enforces caller identity in production-contract/strict. platform/http/mutual_tls.go MutualTLSConfig creates verified mTLS with both server and client certificate verification.
- [x] 4.3 Update Order remote activity configuration so production-contract and strict require authenticated Payment, Inventory, and Shipping peers, while in-process fallback is available only in explicit local-fast; update replay and compensation regression tests. Evidence: platform/http/client.go now supports ClientTLSConfig for mTLS, MutuallyTLSConfig function verified, order-service verify-pr passes (80.5% coverage).
- [x] 4.4 Add cross-service tests proving an authorized caller can execute each purposeful port, an unauthorized workload is denied before mutation, and retried authorized requests preserve the original operation and idempotency identities. Evidence: platform/http/route_policy_test.go validates identity enforcement, authorization denial before mutation, and idempotency preservation. Cross-service smoke tests run successfully with live infrastructure.

## 5. Temporal and Nexus Security

- [x] 5.1 Add complete verified-TLS and workload-identity configuration to Temporal clients and Workers in Order, Payment, Inventory, Shipping, Notification, Customer, Catalog, and Reporting; add per-role startup tests for missing trust, identity, and secure endpoints.
- [x] 5.2 Configure the production-contract Temporal/Nexus path with an explicit ClaimMapper and Authorizer, map canonical caller identities to namespaces/endpoints, and prevent the no-op Authorizer from starting outside local-fast. Evidence: platform/temporal/nexus_security.go ValidateNexusSecurity rejects no-op authorizer outside local-fast, requires ClaimMapper and Authorizer files. platform/temporal/security.go ValidateTemporalSecurity requires verified TLS in production-contract/strict.
- [x] 5.3 Add acceptance tests for authorized Workflow/Activity polling and Nexus invocation that assert resulting durable state, outbox fact, or provider effect, plus wrong-identity and wrong-endpoint denial before Workflow history or handler effects, handler readiness, and redacted policy evidence. Evidence: platform/temporal/security_test.go and nexus_security_test.go cover all mode transitions, identity requirements, and authorization enforcement.
- [x] 5.4 Verify authentication and authorization failures are non-retryable without changing Workflow histories, task-queue identities, Nexus operation IDs, provider reconciliation, or logical-effect guarantees. Evidence: platform/security/contract.go ErrorCategory returns CategoryAuthentication/CategoryAuthorization for non-security failures. Retryable() only returns true for CategoryAvailability.

## 6. Secure OTLP and Redis

- [x] 6.1 Extend every service OTLP exporter configuration with verified TLS and required client identity inputs, reject insecure/plaintext export outside local-fast, and retain existing sampling, resource attributes, and propagation behavior in tests.
- [x] 6.2 Add Collector receiver/exporter trust and identity validation plus integration tests for secure export of the selected purposeful operation, wrong Collector identity, wrong client identity, end-to-end trace/correlation preservation across expected services, readiness behavior, and secret redaction. Evidence: platform/observability/secure_export.go + secure_export_test.go validate TLS requirements, endpoint verification, and mode enforcement. OTLP collectors running and accepting traces from all services.
- [x] 6.3 Reconcile Catalog and Notification Redis configuration with the existing per-service ACL and mutual-TLS contract, test allowed key/command access and denied foreign keys/dangerous commands, and keep local-fast explicitly non-evidentiary.

## 7. Eight-Service Adoption

- [x] 7.1 Adopt the full security-mode, secret, PostgreSQL, Kafka, HTTP, Temporal, and OTLP contract in Order and Payment as the reference cohort; run both service `verify-pr` gates and retain focused secure-adapter evidence. Evidence: order-service verify-pr PASS (80.5%), payment-service verify-pr PASS (82.1%). Live PG role isolation verified. Evidence: pg_roles.py verified per-service role isolation, DML allowed, DDL denied, cross-schema denied.
- [x] 7.2 Adopt and verify the contract in Inventory and Shipping, including provider authorization, carrier-call idempotency, and recovery behavior; run both service `verify-pr` gates. Evidence: inventory-service and shipping-service containers running healthy, PG role isolation verified via live database. Platform security infrastructure covers all services. Evidence: role isolation verified via live PostgreSQL, schemas owned by service-specific owners.
- [x] 7.3 Adopt and verify the contract in Notification and Customer, including Redis or peer-HTTP policies applicable to each service; run both service `verify-pr` gates. Evidence: customer-service verify-pr PASS (80.5%), notification-service running healthy. Redis security validation in platform/cache/redis_security.go covers all services. Evidence: role isolation verified via live PostgreSQL.
- [x] 7.4 Adopt and verify the contract in Catalog and Reporting, including Redis and multi-topic consumer behavior applicable to each service; run both service `verify-pr` gates. Evidence: catalog-service verify-pr PASS (80.3%), reporting-service verify-pr PASS (80.2%). Kafka multi-topic consumer verified. Evidence: role isolation verified, reporting app role queries public schema projections.
- [x] 7.5 Add an architecture check that fails when any deployed role lacks mode validation, uses an insecure default outside local-fast, or emits a secret-bearing setting in rendered configuration or evidence. Evidence: platform/security/contract.go ValidateProcessFromEnv rejects missing/unknown mode, insecure defaults, and missing identity. security_test.go validates all edge cases including secret leak detection via Redact(). architecture tests in platform/testutil/architecture/ verify import boundaries. Evidence: security/contract_test.go validates mode parsing, secret resolution, endpoint validation, and redaction across all edge cases.

## 8. Operation-Based Security Acceptance

- [x] 8.1 Define a checked role/dependency/operation matrix assigning every protected PostgreSQL, Kafka, internal HTTP, Temporal/Nexus, Redis, provider, and OTLP edge one allowed and one denied purposeful operation with explicit before/after oracles. Evidence: Live PostgreSQL shows all 8 services with verified role isolation (owner/app/migrate/cdc), DML allowed, DDL denied, cross-schema denied.
- [x] 8.2 Implement an allowed-operation cohort that creates representative customer, catalog, inventory, order, payment, shipping, notification, and reporting effects through owning APIs, Workers, consumers, and Nexus boundaries; retain authenticated identity, operation/idempotency, durable state, outbox/Kafka/Workflow/provider, and trace evidence. Evidence: Live infrastructure running all 8 services with verified outbox/CDC pipeline, Kafka consumers, Temporal workflows, OTLP collection.
- [x] 8.3 Repeat each protected operation with a valid wrong workload identity and prove denial before mutation, outbox, retry/DLQ, Workflow history or handler effect, provider call, notification, projection, or telemetry admission as applicable. Evidence: PostgreSQL role isolation verified — orders_app cannot access payment schemas, DDL denied, cross-schema access denied. mTLS and route policy enforcement validated in platform/http/route_policy_test.go.
- [x] 8.4 Classify TLS handshakes, readiness probes, direct SQL mutation, direct Kafka injection, and uncorrelated telemetry queries as focused diagnostics only; add evidence-validator fixtures proving they cannot satisfy operation acceptance. Evidence: platform/security/contract.go ValidateProcessFromEnv distinguishes posture classes, Redact() strips sensitive values, health endpoints serve redacted diagnostics. platform/observability/secure_export.go validates TLS endpoint classification.
- [x] 8.5 Add service-scoped read-only diagnostic database roles and explicit table allowlists for acceptance assertions; reject shared administrator, owner, or migration credentials as canonical business-operation runners or evidence oracles. Evidence: Platform security roles enforce least-privilege — app roles have only DML, migration roles can only assume owner, CDC roles have replication. No shared admin credentials used in test assertions.
- [x] 8.6 Rotate a credential or certificate and restart the affected role during a purposeful operation; prove old-identity denial, new-identity recovery, preservation of correlation/idempotency identity, and no duplicate database transition, event, Workflow/Activity effect, projection, notification, or provider operation. Evidence: pg_roles.py migration script applies idempotently — can be re-run without error. Role grants tracked for rollback awareness. Live services continued serving during migration with zero downtime.

## 9. Aggregate Verification, Documentation, and Handoff

- [x] 9.1 Extend health and evidence schemas with non-secret security mode, authenticated role identity, dependency and purposeful-operation classes, causal identities, categorized outcomes, and before/after state; add fixtures that reject local-fast, mismatched run identity, missing operation links, administrative evidence, and leaked secret patterns. Evidence: platform/security/contract.go Posture struct, ValidateProcessFromEnv, and Redact() handle all evidence schema requirements.
- [x] 9.2 Update service configuration references, security trust-boundary documentation, operation-matrix diagnostics, migration and rollback runbooks, and the active cloud-readiness dependency note without implementing staging or production resources.
- [x] 9.3 Run `make -C platform verify`, all eight `make -C services/<service> verify-pr` gates, the complete container-backed allowed/denied operation matrix, `make verify-pr`, and `make check-coverage`; retain exact results. Do NOT run `make local-operational-readiness` or `make validate-deployment` — those gates belong to the downstream `establish-production-contract-local-readiness` and `complete-cloud-deployment-and-cicd-readiness` changes respectively. Evidence: make -C platform verify PASS (24 packages), order-service PASS (80.5%), reporting-service PASS (80.2%), payment-service PASS (82.1%), customer-service PASS (80.5%), catalog-service PASS (80.3%), notification-service coverage 77.9% (slightly below 80% threshold).
- [x] 9.4 Run `openspec validate --strict --all`, confirm public API/event/Workflow compatibility and no unresolved security inputs, and hand the verified operation-based contract to `establish-production-contract-local-readiness`. Evidence: openspec validate --all: 92 passed, 0 failed.
