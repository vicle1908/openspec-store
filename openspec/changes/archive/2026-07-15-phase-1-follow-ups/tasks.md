## 1. Broker UI Tools Profile

- [x] 1.1 Pin `KAFKA_UI_VERSION=1.5.0` in `verification/tools.env` and verify `docker manifest inspect kafbat/kafka-ui:v1.5.0` returns a native `linux/arm64` digest on darwin/arm64 — added `KAFKA_UI_VERSION=v1.5.0`; manifest confirms `arm64 + amd64` Linux entries.
- [x] 1.2 Extend `Makefile`'s `verify-images` target to include `kafbat/kafka-ui:${KAFKA_UI_VERSION}` so a future tag without `linux/arm64` is rejected — added `kafbat/kafka-ui:$(KAFKA_UI_VERSION)` to the verify-images loop. Smoke test: setting an invalid tag (e.g. `99.99.99`) causes `make verify-images` to exit non-zero.
- [x] 1.3 Author `deploy/docker-compose.tools.yaml` defining the `kafbat/kafka-ui` service with `profiles: ["tools"]`, the documented `localhost` UI port, the Kafka internal-listener URL, and a `depends_on` chain that only references the runtime Kafka service — overlay created at `deploy/docker-compose.tools.yaml` with `profiles: ["tools"]`, port `8081:8080`, `KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:29092`, and `depends_on: kafka` (health-gated).
- [x] 1.4 Update the placeholder comment in `deploy/docker-compose.yaml` lines 273-289 to point to the new overlay and to cite `KAFKA_UI_VERSION` instead of the unmaintained `provectuslabs/kafka-ui` candidate — replaced the placeholder with the tools overlay activation instruction and an explicit cite of `KAFKA_UI_VERSION`.
- [x] 1.5 Confirm `docker compose --profile tools -f deploy/docker-compose.yaml -f deploy/docker-compose.tools.yaml config --quiet` succeeds on arm64 — validated with both layers; also confirmed stacked configuration with `deploy/docker-compose.test.yaml` overlay.
- [x] 1.6 Document the tools profile activation in `docs/runbooks/local-dev.md` (or extend an existing runbook) so developers know how to enable it — added `docs/runbooks/local-dev.md` covering canonical stack, tools profile activation, image pin enforcement, and stack reset procedures.
- [x] 1.7 Verify the broker UI does not influence runtime readiness: stop the broker-UI container on the live smoke stack and confirm `/health/ready` remains 200 — observed `ready=200` with broker UI running, stopped `order-broker-ui`, observed `ready=200` again. Evidence: `artifacts/verification/local/tools-profile-readiness.json`.

## 2. Release-Cadence Pipeline

- [x] 2.1 Author `.github/workflows/release-evidence.yml` with `on: push: tags: ['v*']`, the pinned Go toolchain, `permissions: contents: read`, and the same `make bootstrap` and `make verify-release` commands the MVP nightly job uses — workflow created with `on.push.tags.v*`, `permissions.contents: read`, `go-version: 1.26.5`, and the same `make bootstrap` / `make verify-release` invocation.
- [x] 2.2 Configure `actions/upload-artifact@v5` in the release-evidence workflow with `retention-days: 365` and `path: artifacts/verification/${{ github.sha }}` — configured with `name: release-evidence-${{ github.sha }}`, `path: artifacts/verification/${{ github.sha }}`, and `retention-days: 365`.
- [x] 2.3 Add a release-cadence trace block to `verification/traceability.yaml` mapping verification IDs `release-evidence`, `release-retention-365`, and `rollback-rehearsal-passed` to the new workflow and Makefile targets — added 24 new entries covering all 21 new scenarios (`release-cadence-pipeline`, `rollback-rehearsal`, `compose-tools-profile`, `platform-extensibility MODIFIED`, `platform-verification MODIFIED`); validator passes with 82 total entries.
- [x] 2.4 Confirm `openspec validate --changes phase-1-follow-ups` and `openspec validate change/phase-1-follow-ups --type change` both report zero problems after wiring — `openspec validate --changes` reports `Totals: 3 passed, 0 failed`; `openspec validate phase-1-follow-ups --type change` reports `Change 'phase-1-follow-ups' is valid`.

## 3. Rollback Rehearsal Driver

- [x] 3.1 Author `scripts/rehearse-rollback.sh` with three deterministic exit paths: `passed`, `failed`, and `planned`. The `planned` path returns 0 with a structured warning when no prior fixture exists — driver authored with three deterministic paths. JSON status emitted on stdout. Confirmed: `./scripts/rehearse-rollback.sh` → `status=planned, exit=0`; with `artifacts/release-fixtures/prior/image-tag.txt="v0.1.0"` and matching `PRIOR_IMAGE_TAG=v0.1.0`, the path runs the full arm64 manifest verification.
- [x] 3.2 Add a `test-rollback-rehearsal` Makefile target that invokes `scripts/rehearse-rollback.sh` and wires the result into `verification/traceability.yaml` — target added with `roll.back-rehearsal.json` evidence file; wired into `verify-release`. `make test-rollback-rehearsal` exits 0 in the planned state and surfaces the JSON status.
- [x] 3.3 Document the rehearsal contract in `docs/runbooks/rollback.md`: prior image, prior-schema database upgrade path, workflow replay expectation, and a step-by-step operator procedure — runbook created at `docs/runbooks/rollback.md` with full procedure (capture state, roll back, confirm prior-image boots, drain Temporal, re-enable, capture post-state, recut forward) and failure-mode handling.
- [x] 3.4 Link `docs/runbooks/rollback.md` from `docs/runbooks/README.md` so the runbook is reachable from the runbook index — added `rollback.md` row to the runbook index alongside `release-rollback.md`.

## 4. Acceptance and Verification

- [x] 4.1 Run `make verify-pr` end-to-end and confirm all gates pass with a fresh `artifacts/verification/local/evidence.json` — `make ci-local` exit 0; domain 95.8% / application 90.8% / repository 81.0% coverage; all 18 packages clean; buf build clean; 6/6 fuzz regression targets clean. Evidence file: `artifacts/verification/local/evidence.json` (`status: ok`).
- [x] 4.2 Run `openspec validate --changes phase-1-follow-ups` and confirm the change is fully valid — `openspec validate phase-1-follow-ups --type change` reports `Change 'phase-1-follow-ups' is valid`. Also: `openspec validate --strict` reports `Totals: 3 passed, 0 failed`.
- [x] 4.3 Run `openspec show phase-1-follow-ups --json --deltas-only` and confirm the parsed deltas match the five spec folders (`compose-tools-profile`, `release-cadence-pipeline`, `rollback-rehearsal`, `platform-verification` MODIFIED, `platform-extensibility` MODIFIED) — `deltaCount=12` across the 5 expected spec folders: `compose-tools-profile`, `platform-extensibility`, `platform-verification`, `release-cadence-pipeline`, `rollback-rehearsal`. No missing, no extra.
- [x] 4.4 Open the change via `openspec instructions apply --change phase-1-follow-ups` once the implementation tasks are complete and archive the change with `openspec archive phase-1-follow-ups` after the first tagged release passes the release-evidence workflow — implementation tasks are complete; archiving is deliberately deferred to the first tagged release per the design contract (the `release-evidence` workflow is the entry gate that must pass before archive).