## 1. Observability Foundation

- [x] 1.1 Create `src/agent_docs_sync/observability/__init__.py` — call `configure_tracing(otel_endpoint=settings.observability.otel_endpoint, service_name="agent-docs-sync")` from `agent_core.foundation.tracing`. **CRITICAL**: This must be called BEFORE any other OTel imports (it sets `OTEL_SEMCONV_STABILITY_OPT_IN` env var via `os.environ.setdefault()`). Export `get_tracer("docs_sync")` and `get_meter("docs_sync")`. Read settings via `agent_core.foundation.settings.load_settings()`. Handle missing OTel endpoint gracefully (no-op tracer).
- [x] 1.2 Create `src/agent_docs_sync/observability/scorers.py` — implement `DocGenerationCostScorer(Evaluator[dict, dict, dict])` and `DocQualityScorer(Evaluator[dict, dict, dict])` using `pydantic_evals.evaluators.Evaluator` as base class (same pattern as `CostScorer` and `RegressionScorer` in `agent_core.observability.scorers`). Each scorer implements `evaluate(self, ctx: EvaluatorContext) -> float` and `get_default_evaluation_name() -> str`.
- [x] 1.3 Update `src/agent_docs_sync/agent.py` — in `build_doc_sync_agent()`, add hook packs to the HookRegistry: `register_pack(hook_registry, "otel_metrics")`, `register_pack(hook_registry, "structured_audit")`, `register_pack(hook_registry, "langfuse_hooks")`, `register_pack(hook_registry, "mlflow_hooks")`, `register_pack(hook_registry, "cost_tracker", input_rate_per_1k_tokens=0.003, output_rate_per_1k_tokens=0.015)`. Remove the manual `otel_metrics(hook_registry)` and `structured_audit(hook_registry)` calls (they're now done via register_pack).
- [x] 1.4 Add OTel span wrappers to pipeline node handlers in `workflows/full_pipeline.py` (after Phase 3 migration). Each handler wraps its core logic in `tracer.start_as_current_span("docs_sync.{node_name}")` where `tracer = get_tracer("docs_sync")`. Set span attributes: `repo_root`, `node_name`, `iteration`.
- [x] 1.5 Verify: `uv run --directory agent-docs-sync pytest tests/ -x` passes, `uv run --directory agent-docs-sync ruff check src/` clean, manual run of `docs-sync discover --repo .` emits OTel spans (check Langfuse dashboard or OTel collector logs)

## 2. Resilience Layer

- [x] 2.1 Create `src/agent_docs_sync/llm/resilient.py` — `ResilientGateway` class that implements the `LLMGateway` protocol (same interface as `LiteLLMGateway`): wraps `complete()` and `stream()` calls with `CircuitBreaker(name="omniroute", config=BreakerConfig(failure_threshold=5, recovery_timeout_seconds=30))`, `retry_with_jitter(fn, max_attempts=3, backoff_min=0.5, backoff_max=30, retryable=is_transient)`. Use `CircuitBreakerRegistry` to manage the breaker instance. Import `is_transient` from `agent_core.resilience`.
- [x] 2.2 Update `src/agent_docs_sync/llm/gateway.py` — `create_gateway()` returns `ResilientGateway(LiteLLMGateway(...))` instead of raw `LiteLLMGateway`. The `ResilientGateway` constructor takes the inner gateway as a delegate. Keep the existing `create_gateway()` signature unchanged for backward compatibility.
- [x] 2.3 Wire `DegradationManager` in `src/agent_docs_sync/cli.py` — instantiate `DegradationManager(DegradationConfig())` at CLI startup. Before each command, call `degradation.check()`. If level is REDUCED, set `agent_max_iterations=5` in config and log `logger.warning("degradation_reduced")`. If level is NORMAL, use default config values.
- [x] 2.4 Add tests in `tests/test_resilience.py` — test: (1) `CircuitBreaker` transitions CLOSED→OPEN after 5 `record_failure()` calls, (2) `CircuitBreaker.check()` raises `CircuitBreakerOpenError` when OPEN, (3) `retry_with_jitter` retries on `TimeoutError` but not on `ValueError`, (4) `FallbackChain` tries second entry when first breaker is OPEN, (5) `DegradationManager.level` returns REDUCED after `record_error()` × 50 within 60s.
- [x] 2.5 Verify: `uv run --directory agent-docs-sync pytest tests/test_resilience.py -x` passes, manual test with mock LLM returning 500s triggers circuit breaker after 5 attempts

## 3. WorkflowBuilder Migration

- [x] 3.1 Rewrite `src/agent_docs_sync/workflows/full_pipeline.py` — extract the logic inside each `@engine.step` decorated function (the code after the decorator) into plain async handler functions that accept `state: dict[str, Any]` and return `dict[str, Any]`. **CRITICAL**: The current step functions return `SchedulerStepResult(status, output, error)` — the new handlers must NOT call these step-wrapped versions. Instead, copy the inner logic (scanner calls, classifier calls, etc.) into new handler functions. Keep `build_report()` as-is (it's already a plain function).
- [x] 3.2 Update `src/agent_docs_sync/workflows/full_dag.py` — remove the delegation pattern where handlers call `discover_all_step()` and check `result.status.value == "completed"`. Instead, call the new plain handler functions directly. Add conditional routing: `EdgeDescriptor(source="audit", target="generate", condition=EdgeCondition.ON_SUCCESS)` and `EdgeDescriptor(source="audit", target="report", condition=EdgeCondition.ON_FAILURE)` — use `CommandResult(goto="report")` in the audit handler when `stats.gaps_found == 0`. **FIX**: Current `report_handler` passes `state` for all 4 params to `build_report()` — fix to pass actual step results from state.
- [x] 3.3 Delete `src/agent_docs_sync/durable.py` — replaced by WorkflowBuilder with PostgresSaver checkpointing. The `doc_sync_workflow` and its 5 steps are superseded by the new graph. **NOTE**: `durable.py` has a pre-existing syntax error (`except subprocess.TimeoutExpired, FileNotFoundError:` should be `except (subprocess.TimeoutExpired, FileNotFoundError):`) — this confirms it's unused/broken.
- [x] 3.4 Update `src/agent_docs_sync/cli.py` — `sync --full` uses `full_dag.build_full_engine()` with optional `--durable` flag for PostgresSaver via `with create_checkpointer(dsn) as saver: engine = builder.build(checkpointer=saver)`. **NOTE**: `create_checkpointer` is a context manager — must use `with` statement. `audit` uses same engine without LLM generation node (set `state["skip_generate"] = True`). When `--durable` is set but Postgres is unavailable, fall back to no checkpointer with a warning.
- [x] 3.5 Add tests in `tests/test_workflow.py` — test: (1) WorkflowBuilder graph compiles without errors, (2) `discover_handler` returns dict with expected keys, (3) conditional routing works (gaps → generate, no-gaps → report), (4) engine.run() returns WorkflowResult with correct steps_executed, (5) resume from checkpoint works when checkpointer provided.
- [x] 3.6 Verify: `uv run --directory agent-docs-sync pytest tests/ -x` passes, `docs-sync sync --full --repo .` produces same report as before, `docs-sync audit --repo .` works without LLM

## 4. Memory Integration

- [x] 4.1 Create `src/agent_docs_sync/memory/__init__.py` — async factory function `create_memory(dsn: str | None = None) -> Memory` that builds `agent_core.memory.Memory` with: `ContextMemory(max_messages=50)` for context, `ScratchMemory(scratch_dir=str(Path.home() / ".tdt" / "docs-sync" / "scratch"))` for scratch (NOT the default `/tmp/agent-core/scratch`), and `await PostgresMemory.create(dsn)` for long_term if dsn provided, else `None`. **CRITICAL**: `PostgresMemory` is NOT re-exported from `agent_core.memory` — must import from `agent_core.memory.postgres`. `create()` is an async classmethod.
- [x] 4.2 Create `src/agent_docs_sync/memory/sync_state.py` — `SyncState(memory: Memory)` wrapper that reads/writes per-repo sync state to Memory scratch layer (session key `docs-sync:{repo_name}`). Methods: `async load_state(repo_name) -> dict | None`, `async save_state(repo_name, state: dict)`, `async is_stale(repo_name, current_commit) -> bool`. The `is_stale` method compares stored `last_commit` against `current_commit` parameter.
- [x] 4.3 Create `src/agent_docs_sync/memory/metrics.py` — `MetricsStore(memory: Memory)` wrapper for cross-repo metrics in Memory long-term layer (session key `docs-sync:metrics`). Methods: `async record_run(repo_name, cost_usd, docs_generated, quality_score)`, `async get_cost_per_repo() -> dict`, `async get_quality_scores(limit=50) -> list`. **NOTE**: long_term layer requires Postgres — if not configured, these methods should be no-ops (return empty/default values).
- [x] 4.4 Create migration script `src/agent_docs_sync/memory/migrate.py` — `async migrate_yaml_to_memory(memory: Memory, repo_root: Path)`. Reads `.docs-sync-state.yaml`, extracts `auto_mapping` and `invalidation` keys, writes to Memory scratch under session `docs-sync:{repo_name}`, renames YAML to `.docs-sync-state.yaml.bak`. Runs on first use if `memory.scratch.list_keys(session)` returns empty.
- [x] 4.5 Update pipeline handlers in `workflows/full_pipeline.py` (post-3.1) to accept `memory: Memory` via state context. Replace `StateTool._load_state()` calls with `SyncState.load_state()`. Wire `MetricsStore.record_run()` into the report node.
- [x] 4.6 Update `src/agent_docs_sync/tools/state.py` — `StateTool` checks if Memory is available (via state context). If yes, delegates to `SyncState`. If no, falls back to existing YAML manipulation for backward compatibility.
- [x] 4.7 Add tests in `tests/test_memory.py` — test: (1) SyncState save/load round-trip with ScratchMemory, (2) is_stale returns True when commit differs, (3) is_stale returns False when commit matches, (4) MetricsStore.record_run increments total_runs, (5) migrate_yaml_to_memory reads YAML and writes to scratch, (6) migration renames YAML to .bak.
- [x] 4.8 Verify: `uv run --directory agent-docs-sync pytest tests/test_memory.py -x` passes, `docs-sync discover --repo .` writes to `~/.tdt/docs-sync/scratch/docs-sync:agent-core/`, `docs-sync discover --repo . --force` re-reads correctly

## 5. Parallel Multi-Repo

- [x] 5.1 Rewrite `src/agent_docs_sync/multi_repo.py` — add `sync_all_repos_parallel()` using `asyncio.gather` with `asyncio.Semaphore(3)`; each repo runs `build_full_engine().run()` with `thread_id=repo_name`
- [x] 5.2 Update `src/agent_docs_sync/cli.py` — add `--parallel` flag to `sync-all` command; when set, calls `sync_all_repos_parallel()`; default remains sequential
- [x] 5.3 Create aggregate handler — merge per-repo `WorkflowResult` dicts into unified summary (repos_scanned, repos_failed, totals)
- [x] 5.4 Add tests in `tests/test_multi_repo.py` — test parallel execution with mock repos, concurrency limit enforced, per-repo checkpoint isolation, aggregate report
- [x] 5.5 Verify: `uv run pytest tests/test_multi_repo.py -x` passes, `docs-sync sync-all --parallel` runs 3 repos concurrently (check logs for interleaved timestamps), `docs-sync sync-all` runs sequentially

## 6. Cleanup & Documentation

- [x] 6.1 Remove `.docs-sync-state.yaml` references from all non-migration code; update `doc-mapping.yaml` to document Memory-based state
- [x] 6.2 Update `README.md` — document new flags (`--parallel`, `--durable`), observability setup (OTel endpoint, Langfuse config), Memory-based state management
- [x] 6.3 Update `AGENTS.md` — document integration with agent-core features (WorkflowBuilder, Memory, observability, resilience)
- [x] 6.4 Run full test suite: `uv run pytest tests/ -x -v`, `uv run ruff check src/`, `uv run mypy src/ --strict`
- [x] 6.5 Manual end-to-end test: `docs-sync discover --repo ../agent-core`, `docs-sync sync --full --repo ../agent-core`, `docs-sync sync-all --parallel`
