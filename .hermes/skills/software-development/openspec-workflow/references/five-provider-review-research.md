# Historical Five-Provider Review Research — Superseded

This reference is retained for link compatibility. Its old provider/version table and Kimi invocation are superseded by:

- `cli-based-review-workflow.md`
- `five-provider-review-orchestration.md`
- OpenSpec change `repair-seven-cli-review-verification`

Current verified reviewers are Claude, Codex, Antigravity (`agy`), Kimi (`kimi`), OpenCode, Pi, and Goose. Kimi's executable is `kimi`; `fable-5` is a model alias and MUST NOT be used as an executable.

Use each CLI's configured default model/provider. Native subprocess evidence—not wrapper pipeline exit status—is required. The current commands, batch order, status taxonomy, Pi proxy-mode lifecycle repair, and two-round completion gate are defined in the current orchestration references above.
