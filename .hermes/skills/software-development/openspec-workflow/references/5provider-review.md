# Historical Five-Provider Review Guide — Superseded

This filename is retained for compatibility with older links. Do not use its former five-provider commands.

The current verified seven-CLI workflow is defined by:

- `cli-based-review-workflow.md`
- `five-provider-review-orchestration.md`
- `cli-review-troubleshooting.md`
- OpenSpec change `repair-seven-cli-review-verification`

Current external reviewers are Claude, Codex, Antigravity (`agy`), Kimi (`kimi`), OpenCode, Pi, and Goose. Kimi's executable is `kimi`; `fable-5` is a model alias, not an executable.

Use configured default models/providers with no model/provider overrides. Capture native child status before filtering, retain separate stdout/stderr and cryptographic evidence, parse substantive verdicts, run no more than three concurrently, and require a second full round after remediation.
